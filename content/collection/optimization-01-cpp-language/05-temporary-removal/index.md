---
collection_order: 5
date: 2026-03-10
lastmod: 2026-03-28
draft: true
title: "[Optimization(C++) 05] 임시 객체 제거"
slug: temporary-removal
description: "임시 객체 생성이 발생하는 패턴을 진단하고, 연산자 오버로딩·암시적 변환·연속 연산 등에서 임시를 제거하는 패턴을 다룹니다. 컴파일러 출력과 프로파일링으로 임시 비용을 확인하는 방법을 정리하며, 실무 적용 시 주의점과 대안을 제시합니다."
tags:
  - C++
  - Performance
  - Optimization
  - 성능
  - 최적화
  - Memory
  - 메모리
  - Compiler
  - 컴파일러
  - Profiling
  - 프로파일링
  - Benchmark
  - Implementation
  - 구현
  - Code-Quality
  - 코드품질
  - Best-Practices
  - Clean-Code
  - 클린코드
  - OOP
  - 객체지향
  - Time-Complexity
  - 시간복잡도
  - Space-Complexity
  - 공간복잡도
  - Testing
  - 테스트
  - Debugging
  - 디버깅
  - Refactoring
  - 리팩토링
  - Type-Safety
  - Readability
  - Maintainability
  - Modularity
  - Git
  - CI-CD
  - Linux
  - Windows
  - Latency
  - Throughput
  - Backend
  - 백엔드
  - Embedded
  - 임베디드
  - Advanced
  - Deep-Dive
  - 실습
  - Guide
  - 가이드
  - Reference
  - 참고
  - Case-Study
  - Technology
  - 기술
  - Tutorial
  - 튜토리얼
  - Edge-Cases
  - 엣지케이스
  - Pitfalls
  - 함정
  - Data-Structures
  - 자료구조
  - Documentation
  - 문서화
  - Software-Architecture
  - 소프트웨어아키텍처
  - Encapsulation
  - 캡슐화
  - Comparison
  - 비교
---

**임시 객체 제거**란 연산·전달 과정에서 불필요한 임시 생성과 복사/이동을 없애는 것을 말합니다. 본 챕터에서는 연산자 오버로딩·암시적 변환·연속 연산에서 임시가 생기는 패턴을 진단하고, 참조 전달·explicit·in-place 연산(+=) 등으로 제거하는 기법과 컴파일러·프로파일링으로 비용을 확인하는 방법을 정리합니다.

## 임시 객체와 표준 (배경)

C++ 표준은 **temporary materialization** 등으로 "임시가 언제 생성되는지"를 정의합니다. 연산자 오버로딩으로 반환값을 받을 때, 암시적 변환으로 인자를 맞출 때, 값 반환 시(RVO가 없을 때) 임시가 생길 수 있습니다. 컴파일러는 **copy elision**으로 일부 임시를 제거하지만, 사용자 코드가 `operator+` 체이닝·암시적 변환을 유발하면 임시가 남을 수 있어, Low-latency 경로에서는 패턴을 인지하고 제거하는 것이 중요합니다.

> "Temporary objects are created when a prvalue is materialized so that it can be used as a glvalue." — ISO C++ (temporaries). 연산 결과·변환 결과가 "사용되기 위해 구체화"될 때 임시가 생성됩니다.

## 임시 객체 생성 패턴

**연산자 오버로딩**: `a + b`처럼 이항 `operator+`를 쓰면 결과를 담을 **임시 객체**가 생성됩니다. `Matrix c = a + b + d`처럼 체이닝하면 각 `+`마다 임시가 하나씩 생깁니다. 반면 `a += b; a += d;`처럼 `operator+=`를 쓰면 기존 객체 하나만 수정하므로 임시가 필요 없습니다.

```cpp
// 임시 여러 개: a+b 임시, (a+b)+d 임시
Matrix c = a + b + d;

// 임시 없음: in-place
Matrix c = a;
c += b;
c += d;
```

**암시적 변환**: 인자 하나만 받는 생성자(또는 변환 연산자)가 있으면, 컴파일러가 다른 타입을 그 타입으로 자동 변환할 때 **임시**를 만듭니다. 예: `void f(const Big&); f(42);`에서 `Big`가 `int`에서 변환 가능하면 `Big` 임시가 생성됩니다. 변환이 의도치 않았거나 비용이 크면 `explicit`로 암시적 변환을 막는 것이 좋습니다.

```cpp
struct Big { Big(int); };
void f(const Big&);
f(42);  // Big 임시 생성 후 const Big&로 전달

struct ExplicitBig { explicit ExplicitBig(int); };
// f(42);  // 오류: explicit이면 임시 생성 안 됨
```

**함수 인자·반환값**: 인자를 **값**으로 받으면 호출 시 복사(또는 이동)가 일어나고, 반환도 값이면 반환 시 임시가 생길 수 있습니다( RVO/NRVO로 제거되는 경우 제외). 인자를 읽기만 할 때는 `const T&`나 `T&&`로 받아 임시 생성을 줄일 수 있습니다.

**연속 연산**: 위의 `a + b + d`처럼 중간 결과마다 임시가 생깁니다. 중간 결과를 변수에 담아 재사용하거나, `+=` 등 in-place 연산으로 바꾸면 임시를 줄일 수 있습니다.

## 진단 방법

- **컴파일러 출력**: GCC/Clang에서 `-fdump-tree-*` 옵션으로 중간 표현을 덤프하거나, `-S`로 어셈블리를 생성한 뒤 생성자/소멸자 심볼이 몇 번 호출되는지 확인합니다. 인라인되면 한 블록 안에 여러 번 보일 수 있습니다.
- **프로파일링**: 메모리 할당 프로파일러로 특정 타입의 할당 횟수를 보거나, CPU 프로파일러에서 해당 생성자/소멸자 비중을 봅니다.
- **로깅**: 생성자·복사 생성자·이동 생성자에 카운터나 로그를 넣어, 단위 테스트나 벤치마크 실행 시 호출 횟수를 확인합니다. 임시 제거 전후로 횟수가 줄어드는지 검증할 수 있습니다.

## 제거 패턴

- **참조로 전달**: 읽기만 하면 `const T&`, 소유권을 넘기거나 수정할 때는 `T&&`를 사용해 불필요한 복사·임시 생성을 막습니다. `void f(const T&)`는 호출자가 임시를 넘겨도 그 임시의 수명이 함수 종료까지 연장되므로, "읽기 전용" API에 적합합니다.
- **연산 결합**: 중간 결과를 한 번 변수에 담아 재사용하면, 같은 연산을 반복할 때 임시가 반복 생성되지 않습니다. 루프 안에서는 루프 밖에서 한 번만 만들고 재사용하는 패턴을 적용합니다.
- **explicit**: 단일 인자 생성자와 변환 연산자에 `explicit`를 두면 암시적 변환으로 인한 임시가 생기지 않습니다. 의도한 변환만 `static_cast` 등으로 명시적으로 호출하게 됩니다.
- **연산자 설계**: `operator+`는 보통 새 객체를 반환하므로 임시가 생깁니다. 반복적인 덧셈에는 `operator+=`를 제공하고, `a + b`는 `T tmp = a; tmp += b; return tmp;`처럼 구현해 한 번의 명시적 복사만 두는 식으로 설계할 수 있습니다. 사용처에서 `+=`를 쓰도록 가이드하면 임시를 줄일 수 있습니다.

## 실습

- **임시 제거 전/후 마이크로벤치마크**: 동일한 연산을 "임시가 많이 생기는 버전"과 "참조 전달·+=·reserve 등으로 줄인 버전"으로 나누어 실행 시간과 할당 횟수를 비교합니다.
- **실제 코드 리팩토링**: 프로파일러로 임시/할당이 많이 나오는 구간을 찾고, 위 패턴(참조 전달, explicit, += 사용, reserve 등)을 적용한 뒤 같은 벤치마크로 개선을 확인합니다.

## 평가 기준 (학습 성과 목표)

- 연산자 오버로딩(`operator+` vs `operator+=`), 암시적 변환, 값 전달·반환에서 **임시가 생기는 조건**을 설명할 수 있다.
- **참조 전달**(const T&, T&&), **explicit**, **연산 결합·+=**로 임시를 제거할 수 있다.
- 컴파일러 덤프·어셈블리·로깅으로 생성자 호출 횟수를 확인하고, 제거 전/후 벤치마크로 검증할 수 있다.

## 판단 기준 (언제 쓸고 언제 피할지)

| 상황 | 권장 | 비권장 |
|------|------|--------|
| 읽기 전용 인자 | const T& / string_view | 값 전달로 임시 유발 |
| 반복 연산(루프 내) | +=, in-place, 루프 밖 한 번 생성 | 매번 operator+ |
| 단일 인자 생성자 | explicit (의도한 변환만) | 암시적 임시 유발 |
| 연산자 설계 | += 제공, +는 tmp+= 반환 | +만 제공해 임시 다수 |

### 용어 정리

| 용어 | 설명 |
|------|------|
| **temporary materialization** | prvalue가 glvalue로 사용되기 위해 구체화될 때 임시 생성 |
| **explicit** | 단일 인자 생성자·변환 연산자에 붙여 암시적 변환 차단 |

**적용 체크리스트**: (1) 값으로 받는 인자 중 읽기만 하는 것은 const T&/T&&로. (2) 변환 생성자에 explicit. (3) 연속 연산은 += 또는 중간 변수 재사용.

## 비판적 시각: 한계와 트레이드오프

- **참조 전달**: 라이프타임을 호출자가 관리해야 하므로, API 계약을 명확히 한다. 임시를 받아도 수명이 함수 종료까지 연장되는 const T&는 읽기 전용 API에 적합하다.
- **operator+ 제거**: 사용자 편의를 위해 `a + b`를 제공하되, 내부는 `T tmp = a; tmp += b; return tmp;`로 한 번의 복사만 두고, 반복 연산은 += 사용을 권장하는 식으로 균형을 잡을 수 있다.

## 핵심 요약

| 항목 | 요약 |
|------|------|
| 임시 원인 | operator+, 암시적 변환, 값 전달·반환, 연속 연산 |
| 제거 | const T&/T&&, explicit, +=·in-place, reserve |
| 진단 | 어셈블리·덤프·로깅·프로파일러로 호출 횟수 확인 |

### 벤치마크 결과 해석 가이드

| 관찰 | 해석·다음 단계 |
|------|----------------|
| 반복 operator+에서 생성자 호출 다수 | += 또는 중간 변수 재사용으로 전환; reserve와 함께 적용 |
| 값 전달 인자에서 임시 생성 | const T& 또는 T&&로 변경; string_view 등 뷰 타입 검토 |
| 암시적 변환으로 임시 발생 | explicit 추가; 의도한 변환만 명시적 호출 |
| 연속 연산에서 임시 누적 | +=·in-place·루프 밖 한 번 생성 패턴 적용 |

### 자주 묻는 질문 (FAQ)

**Q: 모든 단일 인자 생성자에 explicit를 붙여야 하나요?**  
A: 변환이 의도된 경우(예: 래퍼 타입)만 명시적 변환을 허용하고, 나머지는 explicit로 암시적 임시 생성을 막는 것이 좋습니다. API 설계에 따라 선택합니다.

**Q: operator+를 제거해야 하나요?**  
A: 아니요. operator+는 사용자 편의를 위해 유지하되, 내부는 T tmp = a; tmp += b; return tmp;로 한 번의 복사만 두고, 반복 연산은 += 사용을 권장합니다.

**Q: 참조 전달만 하면 임시가 사라지나요?**  
A: const T&는 호출자가 임시를 넘겨도 수명이 함수 종료까지 연장되므로, "읽기 전용" 인자에서는 임시 생성이 한 번만 일어나고 복사 비용을 줄일 수 있습니다. T&&는 이동을 유도할 때 사용합니다.

### 적용 체크리스트 (실무용)

- [ ] 읽기 전용 인자를 const T&/string_view로 받았는가?
- [ ] 반복 연산에서 += 또는 in-place·루프 밖 한 번 생성을 사용했는가?
- [ ] 단일 인자 생성자에 explicit를 붙였는가?
- [ ] 연산자 설계에서 +=를 제공하고 +는 tmp+= 반환으로 했는가?
- [ ] 컴파일러 덤프·어셈블리·로깅으로 생성자 호출 횟수를 확인했는가?
- [ ] 제거 전/후 벤치마크로 회귀 검증했는가?

### 진단 도구 요약

| 목적 | 도구·방법 |
|------|-----------|
| 생성자 호출 횟수 | 생성자에 로그·카운터, -fdump-tree-*·-S 어셈블리 |
| 할당 횟수 | 메모리 프로파일러, 사용자 정의 할당자 |
| 임시 발생 위치 | 컴파일러 최적화 리포트, 단계별 벤치마크 |

### 학습 후 자가 점검

(1) operator+ vs operator+=에서 임시가 어떻게 달라지는가? (2) 암시적 변환으로 임시가 생기는 조건은? (3) const T&와 값 전달의 차이는? (4) explicit를 붙이는 이유는? (5) 제거 패턴(참조 전달·explicit·+=·reserve)을 적용할 수 있는가?

### 자주 하는 실수

- **값으로 받는 인자 유지**: 읽기만 하는 인자는 const T& 또는 string_view로 바꿔 임시와 복사를 줄입니다.
- **operator+만 제공**: +=를 제공하고 반복 연산에서는 += 사용을 권장합니다.
- **암시적 변환 허용**: 의도하지 않은 변환으로 임시가 생기면 explicit로 막습니다.

### 리팩토링 시 주의

인자를 참조로 바꾸면 호출처에서 임시가 사라지지만, 참조의 수명을 호출자가 보장해야 합니다. API 계약을 명확히 하고, 테스트·벤치마크로 회귀를 확인합니다.

### 추가 읽기 및 관련 챕터

- **챕터 04 (객체 수명)**: 값 반환·RVO; 반환 경로와 임시.
- **챕터 03 (문자열)**: string_view·reserve+append; 문자열 임시 제거.
- **챕터 15 (Parameter Passing)**: by value vs const ref vs rvalue ref.
- **외부**: ISO C++ temporaries, copy elision.

### 이 장을 마치며

임시 객체 제거는 참조 전달·explicit·+=·reserve로 요약됩니다. 진단으로 호출 횟수를 확인하고, 다음 장(06)에서는 템플릿/constexpr를 다룹니다.

**이 장의 학습 목표 재확인**: 임시 원인(operator+·암시적 변환·값 전달)을 설명하고, 참조 전달·explicit·+=로 제거할 수 있으며, 진단·벤치마크로 검증할 수 있어야 합니다.

### 이 장에서 다룬 내용

- 임시와 표준(배경), 연산자·암시적 변환·연속 연산 패턴, cpp 예시(+= vs +, explicit), 제거 패턴·진단 방법·실습, 판단 기준·비판적 시각.
- 벤치마크 해석·FAQ·체크리스트·진단 도구·학습 점검·실수·리팩토링 주의·추가 읽기.

**요약 (한 줄씩)**: 임시 원인 = operator+·암시적 변환·값 전달. 제거 = const T&/T&&·explicit·+=·reserve. 다음은 06(템플릿/constexpr)입니다.

### 상세 예: += vs + 반복

```cpp
// 비권장: 매 반복마다 임시
T sum;
for (const auto& x : vec) sum = sum + x;

// 권장: += 또는 in-place
T sum;
for (const auto& x : vec) sum += x;
```

### 실전 시나리오: 문자열 연결에서 임시 제거

문자열 반복 연결에서 operator+ 대신 reserve + append(또는 +=)를 쓰면 임시와 재할당이 줄어듭니다. 챕터 03(문자열)과 함께 적용하고, 프로파일러로 할당 횟수가 줄었는지 확인합니다.

### 상황별 권장 (요약 표)

| 상황 | 권장 | 비권장 |
|------|------|--------|
| 읽기 전용 인자 | const T& / string_view | 값 전달 |
| 반복 연산(루프 내) | +=, in-place, 루프 밖 한 번 생성 | 매번 operator+ |
| 단일 인자 생성자 | explicit (의도한 변환만) | 암시적 임시 유발 |
| 연산자 설계 | += 제공, +는 tmp+= 반환 | +만 제공 |

### 컴파일러·플랫폼별 참고

- **GCC/Clang**: -fdump-tree-*로 중간 표현에서 임시 생성 확인; -S로 어셈블리에서 생성자 호출 수 확인.
- **MSVC**: 비슷하게 프로파일러·어셈블리로 호출 횟수 확인.

### 이 장을 읽은 후 확인할 수 있는 것

- operator+·암시적 변환·값 전달에서 임시가 생기는 조건을 설명할 수 있다.
- 참조 전달(const T&·T&&)·explicit·+=·reserve로 임시를 제거할 수 있다.
- 컴파일러 덤프·어셈블리·로깅으로 생성자 호출 횟수를 확인하고, 제거 전/후 벤치마크로 검증할 수 있다.
- API 경계(읽기 전용 vs 수정·소유권)에 따라 인자 타입을 선택할 수 있다.

### 용어 정리 (추가)

| 용어 | 설명 |
|------|------|
| **temporary materialization** | prvalue가 구체화될 때 임시 객체가 생성되는 것 |
| **in-place** | 새 객체를 만들지 않고 기존 객체를 수정하는 연산(예: +=) |

### 최종 정리

(1) 임시 원인: operator+·암시적 변환·값 전달·연속 연산. (2) 제거: const T&/T&&·explicit·+=·reserve. (3) 진단: 어셈블리·덤프·로깅·프로파일러. (4) 다음 장 06 = 템플릿/constexpr.

### 선택 플로우 (임시 제거)

1. **읽기 전용 인자** → const T& 또는 string_view.
2. **반복 연산** → +=·in-place·루프 밖 한 번 생성.
3. **단일 인자 생성자** → explicit (의도한 변환만 허용).
4. **연산자** → += 제공, +는 tmp+= 반환; 반복 연산은 += 사용 권장.
5. **검증** → 생성자 호출 횟수·벤치마크로 회귀 확인.

### 게시 전·복습 체크

(1) 도입·정의·예시·비교·마무리가 있는가? (2) 학습 성과 목표·판단 기준·비판적 시각이 있는가? (3) 벤치마크 해석·FAQ·체크리스트·진단 도구가 있는가? (4) 용어 정리·이 장에서 다룬 내용·다음 장 링크가 있는가? (5) 트랙 분량·구성(분량 기준 문서 `CONTENT_LENGTH_TARGETS.md`(트랙 폴더 루트))을 점검했는가? 위를 확인한 뒤 챕터 06(템플릿/constexpr)으로 진행합니다.

### 요약: 이 장의 핵심 메시지

1. **임시 원인**: operator+·암시적 변환·값 전달·연속 연산.
2. **제거**: const T&/T&&·explicit·+=·reserve.
3. **진단**: 생성자 호출 횟수·할당 횟수로 확인.
4. **리팩토링**: 참조 전달·+= 사용·explicit 적용 후 회귀 검증.
5. **다음 장 06**: 템플릿/constexpr로 컴파일 타임 비용 제어.

### 참고 자료

- ISO C++ temporaries, copy elision.
- 챕터 03(문자열), 04(객체 수명), 15(Parameter Passing).

**마무리**: 챕터 05(임시 객체 제거)를 마쳤습니다. 06(템플릿/constexpr)로 넘어가면 컴파일 타임 계산과 비용 제어를 다룹니다. 04(객체 수명)·05(임시 제거)를 함께 적용하면 반환·인자 경로의 복사·임시를 줄일 수 있습니다.

### 정량적 비교 (참고)

반복 operator+ vs +=는 연결 횟수가 많을수록 차이가 큽니다. 값 전달 vs const T&는 호출이 많을 때 복사·임시 차이가 납니다. 실제로는 생성자 로그나 프로파일러로 호출 횟수·할당 횟수를 확인합니다.

### 실무 팁

(1) 프로파일러에서 특정 타입의 생성자·할당이 많이 나오면, 해당 구간이 "읽기 전용 인자"인지 "반복 연산"인지 구분한 뒤 참조 전달·+=·reserve를 적용합니다. (2) explicit를 붙일 때는 의도한 변환(예: 래퍼 from primitive)만 명시적 호출로 허용하고, 나머지는 암시적 임시를 막습니다. (3) 연산자 설계 시 +=를 먼저 제공하고, +는 내부적으로 tmp+=로 구현해 한 번의 복사만 두는 패턴을 권장합니다.

### 이 장의 학습 성과 점검

위 "이 장을 읽은 후 확인할 수 있는 것" 네 항목을 말로 설명할 수 있으면 05의 학습 목표를 달한 것입니다. 06(템플릿/constexpr)에서는 컴파일 타임 계산으로 런타임 비용을 줄이는 방법을 다루므로, 05의 "임시 제거"와 06의 "컴파일 타임 전략"을 함께 적용하면 런타임 생성·복사를 더 줄일 수 있습니다.

### 다음 장(06) 미리보기

템플릿/constexpr에서는 constexpr·consteval로 컴파일 타임에 계산을 끝내고, 템플릿으로 비용을 제어·인라이닝을 유도하는 패턴을 다룹니다. 05에서 다룬 "임시 제거"는 런타임 경로에서의 최적화이고, 06은 "컴파일 타임으로 옮기기"이므로 두 챕터를 연이어 학습하면 효과적입니다.

### 용어·개념 복습

temporary materialization, explicit, in-place 연산(+=), const T& vs 값 전달. 이 용어들을 설명할 수 있으면 05를 잘 소화한 것입니다.

### 구분: 이 장의 범위와 선후 관계

| 구분 | 내용 |
|------|------|
| 이 장(05)의 범위 | 임시 원인·제거 패턴·진단·참조 전달·explicit·+= |
| 다음 장(06) | 템플릿/constexpr: 컴파일 타임 계산·비용 제어 |
| 연계 챕터 | 03(문자열), 04(객체 수명), 15(Parameter Passing) |

### 자주 하는 실수 (추가)

- **참조 전달 후 수명 오류**: const T&로 받은 임시는 함수 종료까지 유지되지만, 포인터/참조를 저장해 두면 댕글링이 됩니다. 저장이 필요하면 복사하거나 소유 타입을 씁니다.
- **+= 없이 +만 제공**: 반복 연산 시 임시가 누적되므로, +=를 제공하고 반복 연산에서는 += 사용을 가이드합니다.
- **과도한 explicit**: 변환이 API의 일부일 때(예: 단위 래퍼)는 explicit를 쓰지 않고, 그 외에는 explicit로 암시적 임시를 막습니다.

### 리팩토링 시나리오: 값 전달 → 참조 전달

기존 API가 void f(T x)일 때, 함수 내부가 읽기만 하면 void f(const T& x)로 바꿀 수 있습니다. 호출처에서 큰 객체나 임시를 넘기면 복사·임시가 사라집니다. 단, f 내부에서 포인터/참조를 저장하지 않는지 확인하고, 테스트·벤치마크로 회귀를 확인합니다.

### 정리 (한 줄씩)

- 임시 원인 = operator+·암시적 변환·값 전달·연속 연산.
- 제거 = const T&/T&&·explicit·+=·reserve.
- 진단 = 생성자 호출 횟수·할당 횟수.
- 다음 장 06 = 템플릿/constexpr.

### 적용 체크리스트 (확장)

- [ ] 읽기 전용 인자를 const T&/string_view로 바꿨는가?
- [ ] 반복 연산에서 += 또는 in-place·루프 밖 한 번 생성을 사용했는가?
- [ ] 단일 인자 생성자에 explicit를 붙였는가?
- [ ] 연산자 설계에서 +=를 제공했는가?
- [ ] 생성자 호출 횟수·할당 횟수로 확인했는가?
- [ ] 제거 전/후 벤치마크로 회귀 검증했는가?
- [ ] 참조 수명을 위반하지 않았는가?

### 참고 자료 (상세)

- ISO C++ temporaries, copy elision.
- 챕터 03(문자열·reserve+append), 04(객체 수명·값 반환), 06(템플릿/constexpr), 15(Parameter Passing).
- 컴파일러 문서(-fdump-tree, -S).

### 진단 도구 요약 (보충)

| 목적 | 도구·방법 |
|------|-----------|
| 생성자 호출 횟수 | 로그·카운터, -S 어셈블리 |
| 할당 횟수 | 메모리 프로파일러 |
| 임시 발생 위치 | -fdump-tree-*, 단계별 벤치마크 |

### 요약 표 (최종)

| 항목 | 요약 |
|------|------|
| 임시 원인 | operator+·암시적 변환·값 전달·연속 연산 |
| 제거 | const T&/T&&·explicit·+=·reserve |
| 진단 | 어셈블리·덤프·로깅·프로파일러 |

### 학습 후 자가 점검 (확장)

(1) operator+와 operator+=에서 임시가 어떻게 다른가? (2) 암시적 변환이 임시를 유발하는 경우는? (3) const T&가 "읽기 전용" API에 적합한 이유는? (4) explicit를 붙이는 이점은? (5) 반복 연산에서 +=를 쓰는 이유는? (6) 제거 전/후에 무엇을 측정하는가?

### 마무리 (최종)

이 장(05)에서는 임시 객체 제거—참조 전달·explicit·+=·reserve—를 다뤘습니다. 진단으로 호출 횟수를 확인하고, 다음 장(06)에서는 템플릿/constexpr를 다룹니다.

- **다음 장(06)**: 템플릿/constexpr — constexpr·consteval·컴파일 타임 비용 제어.
- **연계**: 03(문자열), 04(객체 수명), 15(Parameter Passing).
- **복습**: "이 장을 읽은 후 확인할 수 있는 것" 네 항목을 설명할 수 있으면 05를 완료한 것입니다.

**요약 (한 줄)**: 05 = 임시 제거(참조·explicit·+=·reserve). 06 = 템플릿/constexpr. 위 항목을 설명할 수 있으면 06으로 진행하세요.

**챕터 05 끝.** 다음 장(06)에서는 constexpr·consteval과 템플릿으로 컴파일 타임 계산·비용 제어를 다룹니다.

### 이 장의 평가 기준 재확인

이 장을 읽은 후 다음을 할 수 있어야 합니다: (1) operator+·암시적 변환·값 전달에서 임시가 생기는 조건을 설명할 수 있다. (2) 참조 전달·explicit·+=·reserve로 임시를 제거할 수 있다. (3) 컴파일러 덤프·어셈블리·로깅으로 생성자 호출 횟수를 확인하고, 제거 전/후 벤치마크로 검증할 수 있다. (4) API 경계(읽기 전용 vs 수정)에 따라 인자 타입을 선택할 수 있다. 위를 달성했다면 챕터 06(템플릿/constexpr)로 진행합니다.

### 구분: 이 장의 범위와 선후 관계 (최종)

| 구분 | 내용 |
|------|------|
| 이 장(05)의 범위 | 임시 원인·제거 패턴·진단·참조 전달·explicit·+= |
| 다음 장(06) | 템플릿/constexpr |
| 연계 챕터 | 03(문자열), 04(객체 수명), 15(Parameter Passing) |

### 참고

임시 제거는 "측정 후 적용"이 중요합니다. 프로파일러로 생성자·할당이 많이 나오는 구간을 찾고, 참조 전달·+=·explicit를 적용한 뒤 동일 벤치마크로 개선을 확인합니다. 04(객체 수명)와 05(임시 제거)를 함께 적용하면 반환·인자 경로 모두에서 복사·임시를 줄일 수 있습니다.

### 정리 (최종)

(1) 임시 원인: operator+·암시적 변환·값 전달·연속 연산. (2) 제거: const T&/T&&·explicit·+=·reserve. (3) 진단: 생성자 호출 횟수·할당 횟수. (4) 검증: 제거 전/후 벤치마크. (5) 다음 장 06 = 템플릿/constexpr.

(이상으로 챕터 05 임시 객체 제거를 마칩니다. 다음 장 06에서는 템플릿/constexpr를 다룹니다.)

- **다음**: [템플릿/constexpr](/post/cpp-optimization/templates-constexpr/) (챕터 06)

### 요약 표 (한눈에)

| 항목 | 요약 |
|------|------|
| 임시 원인 | operator+·암시적 변환·값 전달·연속 연산 |
| 제거 | const T&/T&&·explicit·+=·reserve |
| 진단 | 로그·어셈블리·프로파일러 |

### 마무리 (한 줄)

05 = 임시 제거(참조·explicit·+=·reserve). 06 = 템플릿/constexpr. 위 평가 기준을 설명할 수 있으면 06으로 넘어가세요.

**챕터 05 완료 조건**: 임시 원인·제거 패턴·진단을 설명하고, 참조 전달·explicit·+=를 적용·검증할 수 있으면 완료입니다.

**참고**: 04(객체 수명)와 05(임시 제거)를 연이어 적용하면 반환·인자 경로의 복사·임시를 체계적으로 줄일 수 있습니다. 06(템플릿/constexpr)에서는 컴파일 타임으로 비용을 옮기는 방법을 다룹니다.

### 정리 (최종)

(1) 임시 원인 = operator+·암시적 변환·값 전달·연속 연산. (2) 제거 = const T&/T&&·explicit·+=·reserve. (3) 진단 = 생성자 호출 횟수·할당 횟수. (4) 검증 = 제거 전/후 벤치마크. (5) 다음 06 = 템플릿/constexpr. (6) 04·05 함께 적용 시 반환·인자 경로 최적화.

**다음 단계**: 06(템플릿/constexpr)에서 constexpr·consteval을 배우면, 런타임 비용을 컴파일 타임으로 옮길 수 있습니다. 05(임시 제거)와 06(템플릿/constexpr)을 함께 적용하면 런타임 생성·복사와 컴파일 타임 전략을 모두 활용할 수 있습니다.

- **복습**: 임시 원인·제거 패턴·참조 전달·explicit·+=를 설명할 수 있으면 05를 완료한 것입니다.
- **다음 장(06)**: 템플릿/constexpr — constexpr·consteval·컴파일 타임 비용 제어.

(챕터 05 임시 객체 제거 끝. 다음은 06 템플릿/constexpr입니다.)

**요약 (한 줄)**: 05 = 임시 제거(참조·explicit·+=·reserve). 06 = 템플릿/constexpr. 위 항목을 설명할 수 있으면 06으로 진행하세요.

- **이 장의 범위**: 임시 원인·제거 패턴·진단·참조 전달·explicit·+=.
- **다음**: [템플릿/constexpr](/post/cpp-optimization/templates-constexpr/) (챕터 06).

**참고**: 임시 제거는 프로파일러로 "어디서" 생성·할당이 나오는지 확인한 뒤, 해당 구간에 참조 전달·+=·explicit·reserve를 적용합니다. 06(템플릿/constexpr)에서는 컴파일 타임 계산으로 런타임 비용을 줄이므로, 05와 06을 연이어 학습하면 런타임·컴파일 타임 양쪽 최적화를 다룰 수 있습니다.

**정리 (최종)**: (1) 임시 원인·제거·진단·검증. (2) 04·05 함께 적용. (3) 다음 06 = 템플릿/constexpr.

**챕터 05 끝.** 다음 장(06)에서는 constexpr·consteval과 템플릿으로 컴파일 타임 계산·비용 제어를 다룹니다. 04·05·06을 연이어 학습하면 반환·인자·컴파일 타임 전략까지 체계적으로 다룰 수 있습니다.

- **이 장의 범위**: 임시 원인·제거 패턴·진단·참조 전달·explicit·+=.
- **다음 장(06)**: 템플릿/constexpr — constexpr·consteval·컴파일 타임 비용 제어.
- **복습**: "이 장을 읽은 후 확인할 수 있는 것" 네 항목을 설명할 수 있으면 05를 완료한 것입니다.

**요약 (한 줄)**: 05 = 임시 제거. 06 = 템플릿/constexpr. 위 항목을 설명할 수 있으면 06으로 진행하세요.

(챕터 05 임시 객체 제거 끝. 다음은 06 템플릿/constexpr입니다.)

- **다음**: [템플릿/constexpr](/post/cpp-optimization/templates-constexpr/) (챕터 06).

---

### 이 장에서 다룬 내용 (전체)

- 임시와 표준(배경), 연산자·암시적 변환·연속 연산 패턴, cpp 예시, 제거 패턴·진단·실습, 판단 기준·비판적 시각.
- 벤치마크 해석·FAQ·체크리스트·진단 도구·학습 점검·실수·리팩토링 주의·추가 읽기·상세 예·실전 시나리오·상황별 권장·컴파일러 참고·확인 항목·용어 추가·최종 정리·선택 플로우·게시 전 체크·핵심 메시지·참고 자료.

**챕터 05 정리**: 임시 제거 = 참조 전달 + explicit + +=·reserve. 다음은 06(템플릿/constexpr)입니다.

(이상으로 챕터 05 임시 객체 제거를 마칩니다.)

→ [템플릿/constexpr](/post/cpp-optimization/templates-constexpr/) (챕터 06)

## 다음 장에서는

**이전 장**: [객체 수명 최적화](/post/cpp-optimization/object-lifetime/) (챕터 04)

**템플릿/constexpr**를 다룹니다. constexpr·consteval로 컴파일 타임 계산을 하고, 템플릿으로 비용을 제어·인라이닝을 유도하는 패턴을 정리합니다.
