---
collection_order: 7
date: 2026-03-10
lastmod: 2026-03-28
draft: true
title: "[Optimization(C++) 07] Modern C++ 기능"
slug: modern-cpp-features
description: "C++17/20/23의 성능 관련 기능인 ranges, concepts, modules 등을 정리합니다. 새 표준 기능의 비용·이점을 측정하고, Low-latency 코드에서의 활용 기준을 다루며, 컴파일러 지원과 마이그레이션 시 주의점을 제시합니다."
tags:
  - C++
  - Performance
  - Optimization
  - 성능
  - 최적화
  - Compiler
  - 컴파일러
  - Memory
  - 메모리
  - Implementation
  - 구현
  - Code-Quality
  - 코드품질
  - Best-Practices
  - Clean-Code
  - 클린코드
  - Type-Safety
  - Profiling
  - 프로파일링
  - Benchmark
  - Data-Structures
  - 자료구조
  - Time-Complexity
  - 시간복잡도
  - Testing
  - 테스트
  - Refactoring
  - 리팩토링
  - Readability
  - Maintainability
  - Modularity
  - Git
  - CI-CD
  - Linux
  - Windows
  - Latency
  - Throughput
  - Backend
  - 백엔드
  - Embedded
  - 임베디드
  - Advanced
  - Deep-Dive
  - 실습
  - Guide
  - 가이드
  - Reference
  - 참고
  - Case-Study
  - Technology
  - 기술
  - Tutorial
  - 튜토리얼
  - Comparison
  - 비교
  - Migration
  - 마이그레이션
  - Software-Architecture
  - 소프트웨어아키텍처
  - Design-Pattern
  - 디자인패턴
  - Documentation
  - 문서화
  - Edge-Cases
  - 엣지케이스
  - Pitfalls
  - 함정
  - Debugging
  - 디버깅
---

**Modern C++ 기능**이란 C++17/20/23에서 도입된 ranges, concepts, modules 등 성능·표현력에 영향을 주는 기능을 말합니다. 본 챕터에서는 이들의 비용과 이점을 정리하고, Low-latency 코드에서 활용할 때 측정·기준을 다룹니다.

## C++17/20/23 성능 관련 기능 (역사·배경)

**Ranges**는 C++20에서 표준에 들어왔고, 범위 기반 연산과 뷰·지연 평가를 제공합니다. **Concepts**도 C++20에서 도입되어 템플릿 제약을 타입 시스템으로 표현합니다. **Modules**는 C++20에서 표준화되어 헤더 대신 모듈 단위로 인터페이스를 나누어 컴파일 시간과 노출 범위를 줄입니다. 각 기능은 "표현력·안전성·빌드"에 이점이 있으나, Low-latency에서는 **런타임 오버헤드**가 있는지(예: ranges 파이프라인 인라인, concepts의 런타임 비용 없음)를 구분해 핫패스에 쓸지 결정해야 합니다.

> "A range is a sequence of elements that can be iterated. A view is a non-owning range that is O(1) to copy and move." — cppreference (std::ranges). 뷰는 복사가 저렴하고 지연 평가되므로, 파이프라인으로 연결해도 중간 컨테이너 할당을 줄일 수 있습니다.

## Ranges (C++20)

Ranges 라이브러리는 **범위(range)**를 첫 등장 인자로 두고, **뷰(view)**를 파이프 `|`로 연결해 연산을 표현합니다. 많은 뷰는 **지연 평가**되어, 최종적으로 소비(순회·수집)될 때만 연산이 실행됩니다. 뷰는 대체로 **복사 없이** 원본 범위를 참조하므로, 중간 컨테이너 할당을 줄일 수 있습니다. 메모리·반복자 관점에서 보면, 뷰는 "어딘가의 연속/범위를 가리키는 lightweight 객체"입니다.

**ranges 파이프라인 예시**: 뷰를 파이프 `|`로 연결하면 중간 결과 없이 최종 소비 시점에만 연산이 실행됩니다.

```cpp
#include <ranges>
#include <vector>

std::vector<int> v = {1, 2, 3, 4, 5};
auto r = v | std::views::filter([](int x) { return x % 2 == 0; })
           | std::views::transform([](int x) { return x * 2; });
// r은 뷰; 순회할 때만 filter·transform 실행. 중간 vector 할당 없음.
for (int x : r) { /* 4, 8 */ }
```

**views vs ranges**: 뷰는 non-owning이고 복사가 저렴하며, range는 데이터를 소유할 수 있습니다. 벤치마크에서는 "기존 for 루프 + 수동 연산"과 "ranges 파이프라인"을 같은 알고리즘으로 비교해, 인라인·임시 생성·반복자 오버헤드가 얼마나 나오는지 확인할 수 있습니다. 구현체와 최적화 수준에 따라 파이프라인이 충분히 인라인되면 오버헤드가 거의 없을 수도 있고, 그렇지 않으면 µs 단위 경로에서 눈에 띌 수 있습니다. **핫패스**에서는 가능하면 한 번 벤치마크해 보고, 오버헤드가 허용 범위인지 확인한 뒤 사용하는 것이 안전합니다.

## Concepts (C++20)

Concepts는 템플릿 파라미터에 **제약**을 걸어, 컴파일 타임에 타입이 요구사항을 만족하는지 검사합니다. 에러 메시지가 "이 제약을 만족하지 않는다"로 명확해지고, 오버로드 해석도 제약으로 더 잘 짜일 수 있습니다. **인스턴스화·인라이닝** 측면에서는 "제약만 추가된 템플릿"에 가깝기 때문에, 만족하는 타입에 대해서는 기존 템플릿과 동일하게 인라인·최적화되며 **추가 런타임 오버헤드는 없습니다**. 성능 요약하면, concepts는 주로 표현력과 컴파일 타임 검사에 이점이 있고, 런타임 비용은 템플릿과 동일한 수준으로 보면 됩니다.

## Modules (C++20)

Modules는 **번역 단위** 단위로 인터페이스와 구현을 나누어, 헤더를 반복 파싱하지 않고 **컴파일 시간**을 줄이고 **빌드 캐시**를 활용할 수 있게 합니다. 모듈에서 export하는 선언만 보이므로, **헤더 전체를 읽는 것보다 노출 범위가 작아져** 인라인·최적화 경계가 더 예측 가능해질 수 있습니다. 다만 아직 **구현체·빌드 시스템·도구 지원**이 플랫폼마다 다르므로, 도입 시 빌드 설정과 성능을 프로젝트에서 측정해 보는 것이 좋습니다.

## 기타 C++17/20/23 기능 (성능 관점)

- **if constexpr**: 컴파일 타임에 분기가 결정되므로, 선택되지 않은 경로의 코드는 생성되지 않고 런타임 분기도 없습니다. 템플릿 안에서 타입·상수에 따라 다른 구현을 택할 때 유용합니다.
- **구조화된 바인딩**: 튜플·구조체 멤버를 바인딩할 때 복사/참조를 명시할 수 있어, 불필요한 복사를 줄이기 쉽습니다. `auto [a, b] = get_pair();`에서 `get_pair()` 반환 타입이 값이면 한 번의 복사/이동만 일어납니다.
- **Designated initializers**: 멤버 초기화 순서와 의도를 명확히 할 수 있으며, 불필요한 기본 생성 후 대입을 줄일 수 있습니다.
- **[[likely]] / [[unlikely]]**: 분기 예측 힌트로, CPU가 해당 경로를 선호하도록 할 수 있습니다. 효과는 플랫폼·컴파일러에 따라 다르므로, 중요한 경로에만 쓰고 벤치마크로 확인하는 것이 좋습니다.
- **std::format, constexpr 확장**: 포맷·문자열 처리와 constexpr 가능 범위가 넓어져, 상수 문자열 생성·포맷을 컴파일 타임에 처리할 수 있는 경우가 늘었습니다. 자세한 포맷 비용은 챕터 03(문자열 최적화)을 참고합니다.

## 평가 기준 (학습 성과 목표)

- **Ranges**의 지연 평가·뷰(non-owning) 특성을 설명하고, 핫패스에서 벤치마크 후 오버헤드가 허용되는지 판단할 수 있다.
- **Concepts**가 런타임 오버헤드 없이 컴파일 타임 제약만 추가함을 설명할 수 있다.
- **Modules**가 컴파일 시간·노출 범위에 미치는 영향을 설명하고, 도입 시 빌드·성능을 측정할 수 있다.
- if constexpr, 구조화된 바인딩, [[likely]]/[[unlikely]] 등이 성능에 미치는 맥락을 구분할 수 있다.

## 판단 기준 (언제 쓸고 언제 피할지)

| 상황 | 권장 | 비권장 |
|------|------|--------|
| ranges 파이프라인 | 벤치마크 후 오버헤드 허용 시 | µs 경로에 무검증 도입 |
| 템플릿 제약 | concepts로 가독성·에러 메시지 개선 | 런타임 비용 기대 (없음) |
| 빌드 시간·모듈화 | modules 실험·측정 후 도입 | 도구 미지원 환경에서 강제 |

**적용 체크리스트**: (1) 핫패스에 ranges 넣기 전 동일 알고리즘으로 벤치마크. (2) concepts는 표현력·검사용, 런타임 비용 없음 인지. (3) [[likely]]/[[unlikely]]는 중요 경로에만, 효과는 측정으로 확인.

## 비판적 시각: 한계와 트레이드오프

- **Ranges**: 구현체·최적화에 따라 파이프라인 오버헤드가 있을 수 있다. "무조건 for 루프"가 아니라, 가독성과 성능을 벤치마크로 맞보는 것이 합리적이다.
- **Modules**: 빌드 시스템·컴파일러 지원이 아직 불균일하므로, 프로젝트별로 도입 가능 여부와 이득을 평가해야 한다.

## 핵심 요약

| 항목 | 요약 |
|------|------|
| Ranges | 지연 평가·뷰, 중간 할당 감소; 핫패스는 벤치마크 후 사용 |
| Concepts | 컴파일 타임 제약, 런타임 오버헤드 없음 |
| Modules | 컴파일 시간·노출 감소, 도구 지원 확인 필요 |
| 기타 | if constexpr·구조화 바인딩·[[likely]] 등 상황별 활용 |

### 용어 정리

| 용어 | 설명 |
|------|------|
| **range** | 순회 가능한 요소 시퀀스; C++20 std::ranges |
| **view** | non-owning, 복사 O(1), 지연 평가되는 range |
| **concept** | 템플릿 파라미터 제약; 컴파일 타임 검사, 런타임 비용 없음 |

### 벤치마크 결과 해석 가이드

| 관찰 | 해석·다음 단계 |
|------|----------------|
| ranges 파이프라인이 for 루프보다 느림 | 구현체·컴파일러에 따라 다름; 동일 알고리즘으로 벤치마크 후 도입 |
| concepts 도입 후 런타임 차이 없음 | 정상; concepts는 컴파일 타임 제약, 런타임 오버헤드 없음 |
| modules 도입 후 빌드 시간 감소 | 컴파일 시간 절감; 도구 지원 확인 |

### 자주 묻는 질문 (FAQ)

**Q: ranges가 항상 for 루프보다 느리나요?**  
A: 구현체·최적화에 따라 다릅니다. 동일 알고리즘으로 벤치마크한 뒤, 허용 가능한 오버헤드일 때만 핫패스에 도입합니다.

**Q: concepts는 런타임 비용이 있나요?**  
A: 없습니다. 컴파일 타임 제약만 추가되므로, 가독성·에러 메시지 개선용으로 사용합니다.

**Q: modules를 지금 도입해야 하나요?**  
A: 빌드 시스템·컴파일러 지원이 프로젝트마다 다릅니다. 실험·측정 후 도입 가능 여부를 평가합니다.

### 적용 체크리스트 (실무용)

- [ ] 핫패스에 ranges를 넣기 전 동일 알고리즘으로 벤치마크했는가?
- [ ] concepts는 런타임 비용 없음을 인지하고 있는가?
- [ ] modules 도입 시 도구 지원을 확인했는가?
- [ ] [[likely]]/[[unlikely]]는 효과를 측정한 뒤 적용했는가?
- [ ] 변경 후 벤치마크·빌드 시간으로 회귀 검증했는가?

### 진단 도구 요약

| 목적 | 도구·방법 |
|------|-----------|
| ranges vs for 성능 | 격리 벤치마크(동일 알고리즘) |
| concepts | 런타임 비용 없음; 컴파일 시간만 확인 |
| modules | 빌드 시간 측정, 도구 지원 확인 |

### 학습 후 자가 점검

(1) Ranges·Concepts·Modules의 비용·이점을 구분할 수 있는가? (2) ranges 파이프라인을 핫패스에 넣기 전 무엇을 하는가? (3) concepts가 런타임 오버헤드가 없는 이유는? (4) modules 도입 시 확인할 것은? (5) if constexpr·[[likely]] 등을 상황별로 선택할 수 있는가?

### 자주 하는 실수

- **ranges를 핫패스에 무검증 도입**: 벤치마크 후 오버헤드가 허용될 때만 도입합니다.
- **concepts에 런타임 비용 기대**: concepts는 컴파일 타임만; 런타임 비용 없음.
- **modules를 도구 미지원 환경에서 강제**: 지원 여부와 빌드 시간 이득을 먼저 확인합니다.

### 리팩토링 시 주의

기존 for 루프를 ranges로 바꿀 때 동일 알고리즘인지 확인하고, 벤치마크로 회귀가 없는지 검증합니다. modules 도입 시 include 순서·헤더 노출이 바뀌므로 빌드·테스트를 충분히 수행합니다.

### 추가 읽기 및 관련 챕터

- **챕터 06 (템플릿/constexpr)**: 컴파일 타임 전략과 연계.
- **챕터 08 (코루틴)**: C++20 비동기 기능.
- **챕터 12 (span과 뷰)**: 뷰 패턴·non-owning.
- **외부**: C++20 Ranges, Concepts, C++20 Modules.

### 이 장을 마치며

Modern C++ 기능은 Ranges·Concepts·Modules의 비용·이점과 상황별 활용으로 요약됩니다. 다음 장(08)에서는 코루틴 성능을 다룹니다.

**이 장의 학습 목표 재확인**: Ranges·Concepts·Modules의 비용·이점을 설명하고, 핫패스에 도입 전 벤치마크·도구 지원 확인을 할 수 있으며, [[likely]]/if constexpr 등을 상황별로 적용할 수 있어야 합니다.

### 이 장에서 다룬 내용

- C++17/20/23 성능 관련 기능 배경, Ranges·Concepts·Modules 비용·이점, ranges 파이프라인 cpp 예시, 판단 기준·비판적 시각.
- 벤치마크 해석·FAQ·체크리스트·진단 도구·학습 점검·실수·리팩토링 주의·추가 읽기.

**요약 (한 줄씩)**: Ranges = 지연 평가·뷰; 핫패스는 벤치마크 후. Concepts = 컴파일 타임 제약, 런타임 비용 없음. Modules = 빌드 시간·도구 확인. 다음은 08(코루틴)입니다.

### 상세 예: ranges 파이프라인

```cpp
// C++20 ranges: 파이프라인으로 필터·변환·취소
auto result = vec | std::views::filter(pred) | std::views::take(10);
// 동일 로직을 for 루프로 구현한 버전과 벤치마크 비교 권장
```

### 실전 시나리오: ranges 도입

기존 for 루프를 ranges 파이프라인으로 바꿀 때, 동일 알고리즘으로 벤치마크를 만들어 오버헤드가 허용되는지 확인합니다. 허용되면 도입하고, 회귀 벤치마크를 CI에 넣습니다.

### 상황별 권장 (요약 표)

| 상황 | 권장 | 비권장 |
|------|------|--------|
| ranges 파이프라인 | 벤치마크 후 오버헤드 허용 시 | µs 경로에 무검증 도입 |
| concepts | 가독성·에러 메시지 개선 | 런타임 비용 기대 |
| modules | 실험·측정 후 도입 | 도구 미지원에서 강제 |
| [[likely]]/[[unlikely]] | 중요 경로에만, 측정 후 | 무분별 적용 |

### 컴파일러·플랫폼별 참고

- **GCC/Clang**: C++20 ranges·concepts 지원. modules는 버전에 따라 다름.
- **MSVC**: C++20 ranges·concepts·modules 지원. 프로젝트별로 확인.

### 이 장을 읽은 후 확인할 수 있는 것

- Ranges·Concepts·Modules의 비용·이점을 설명할 수 있다.
- 핫패스에 ranges를 넣기 전 벤치마크하고, concepts는 런타임 비용 없음을 설명할 수 있다.
- modules 도입 시 도구 지원·빌드 시간을 확인할 수 있다.
- if constexpr·[[likely]] 등을 상황별로 적용할 수 있다.

### 용어 정리 (추가)

| 용어 | 설명 |
|------|------|
| **range** | 순회 가능한 시퀀스; C++20 std::ranges |
| **view** | non-owning, O(1) 복사, 지연 평가 range |
| **concept** | 템플릿 제약; 컴파일 타임만, 런타임 비용 없음 |

### 최종 정리

(1) Ranges = 지연 평가·뷰; 핫패스는 벤치마크 후. (2) Concepts = 컴파일 타임 제약, 런타임 비용 없음. (3) Modules = 빌드 시간·도구 확인. (4) 다음 장 08 = 코루틴 성능.

### 선택 플로우 (Modern C++ 기능)

1. **ranges** 도입 전 동일 알고리즘으로 벤치마크한다.
2. **concepts**는 가독성·에러 메시지용; 런타임 비용 없음을 인지한다.
3. **modules**는 도구 지원·빌드 시간을 측정한 뒤 도입한다.
4. **[[likely]]/[[unlikely]]**는 중요 경로에만, 효과를 측정한 뒤 적용한다.
5. **검증**: 벤치마크·빌드 시간으로 회귀 확인한다.

### 게시 전·복습 체크

(1) 도입·정의·예시·비교·마무리가 있는가? (2) 학습 성과 목표·판단 기준·비판적 시각이 있는가? (3) 벤치마크 해석·FAQ·체크리스트·진단 도구가 있는가? (4) 용어 정리·이 장에서 다룬 내용·다음 장 링크가 있는가? (5) 트랙 분량·구성(분량 기준 문서 `CONTENT_LENGTH_TARGETS.md`(트랙 폴더 루트))을 점검했는가? 위를 확인한 뒤 챕터 08(코루틴 성능)으로 진행합니다.

### 요약: 이 장의 핵심 메시지

1. **Ranges**는 지연 평가·뷰로 중간 할당을 줄일 수 있으나, 구현체에 따라 오버헤드가 있을 수 있어 핫패스는 벤치마크 후 도입한다.
2. **Concepts**는 컴파일 타임 제약으로 런타임 오버헤드가 없다.
3. **Modules**는 컴파일 시간·노출 감소에 도움이 될 수 있으나, 도구 지원을 확인한다.
4. **if constexpr·[[likely]]** 등은 상황별로 활용하고, 효과는 측정으로 확인한다.
5. **다음 장 08**: 코루틴 성능.

### 참고 자료

- C++20 Ranges, Concepts, Modules.
- 챕터 06(템플릿/constexpr), 08(코루틴), 12(span과 뷰).

**마무리**: 챕터 07(Modern C++ 기능)을 마쳤습니다. 08(코루틴 성능)로 넘어가면 C++20 코루틴의 비용·활용 기준을 다룹니다.

### 이 장에서 다룬 내용 (전체)

- C++17/20/23 성능 관련 기능 배경, Ranges·Concepts·Modules 비용·이점, ranges cpp 예시, 판단 기준·비판적 시각.
- 벤치마크 해석·FAQ·체크리스트·진단 도구·학습 점검·실수·리팩토링 주의·추가 읽기·상세 예·실전 시나리오·상황별 권장·컴파일러 참고·확인 항목·용어 추가·최종 정리·선택 플로우·게시 전 체크·핵심 메시지·참고 자료.

**챕터 07 정리**: Modern C++ = Ranges(벤치마크 후)·Concepts(런타임 비용 없음)·Modules(도구 확인). 다음은 08(코루틴)입니다.

(이상으로 챕터 07 Modern C++ 기능을 마칩니다.)

### 정량적 비교 (Ranges vs for 루프)

| 측면 | ranges 파이프라인 | 전통적 for 루프 |
|------|-------------------|-----------------|
| 가독성 | 선언적, 파이프 형태 | 명령형, 제어 흐름 명시 |
| 최적화 | 구현체·컴파일러 의존 | 예측 가능한 경로 많음 |
| 벤치마크 | 동일 알고리즘으로 비교 필수 | 기준선으로 활용 |

실제 수치는 프로젝트·컴파일러·데이터 크기에 따라 다르므로, 도입 전 격리 벤치마크를 수행합니다.

### 실무 팁

- **ranges**: 먼저 비핫패스·유틸 코드에 적용해 경험을 쌓고, 핫패스는 벤치마크 후 결정합니다.
- **concepts**: 에러 메시지가 나빠지는 구간에만 적용해도 이득이 큽니다.
- **modules**: 헤더가 많은 라이브러리부터 점진적으로 모듈화하고, 빌드 시간을 측정합니다.
- **[[likely]]/[[unlikely]]**: 분기 예측이 중요한 한두 군데만 적용하고, 그 이상은 프로파일로 확인합니다.

### 학습 성과 점검

다음 질문에 답할 수 있으면 이 장의 목표를 달성한 것입니다. (1) Ranges의 지연 평가·뷰 특성을 설명할 수 있는가? (2) Concepts가 런타임 비용이 없는 이유는? (3) Modules 도입 시 확인할 항목은? (4) 핫패스에 ranges를 넣기 전 수행할 작업은? (5) if constexpr과 [[likely]]의 사용 시기를 구분할 수 있는가?

### 다음 장(08) 미리보기

챕터 08에서는 **코루틴 성능**을 다룹니다. C++20 코루틴의 suspend/resume 비용, 프레임 할당, 저지연 경로에서의 사용 기준을 정리합니다. 07의 Ranges·Concepts와 달리, 코루틴은 런타임 오버헤드가 있으므로 측정이 중요합니다.

### 용어·개념 복습

| 용어 | 한 줄 요약 |
|------|------------|
| range | 순회 가능한 시퀀스; C++20 std::ranges |
| view | non-owning, O(1) 복사, 지연 평가 |
| concept | 템플릿 제약; 컴파일 타임만 |
| module | 헤더 대체, 빌드 시간·노출 감소 |

### 구분 표: 언제 무엇을 쓸지

| 목표 | 권장 | 비권장 |
|------|------|--------|
| 선언적 순회 | ranges(벤치마크 후) | µs 경로에 무검증 |
| 타입 제약·에러 메시지 | concepts | 런타임 비용 기대 |
| 빌드 시간 단축 | modules(도구 확인 후) | 미지원 환경 강제 |
| 분기 힌트 | [[likely]]/[[unlikely]](측정 후) | 무분별 적용 |

### 자주 하는 실수 (확장)

- **ranges를 "무조건 더 나은 것"으로 간주**: 구현체에 따라 for 루프보다 느릴 수 있으므로, 항상 벤치마크로 확인합니다.
- **concepts를 런타임 검사로 오해**: concepts는 컴파일 타임 제약만 제공합니다.
- **modules를 모든 프로젝트에 적용**: 빌드 시스템·컴파일러 지원이 준비된 환경에서만 도입합니다.
- **[[likely]] 남용**: 한두 개의 핵심 분기만 힌트하고, 나머지는 프로파일러로 확인합니다.

### 리팩토링 시나리오

기존 for 루프를 ranges로 바꿀 때: (1) 동일 알고리즘인지 검증, (2) 격리 벤치마크로 오버헤드 측정, (3) 허용 가능하면 도입 후 회귀 벤치마크를 CI에 추가. modules 도입 시: (1) 한 라이브러리/모듈 단위로 나누어, (2) 빌드 시간·의존성 그래프를 측정, (3) 점진적으로 확대합니다.

### 정리

챕터 07에서는 Modern C++ 기능(Ranges·Concepts·Modules·if constexpr·[[likely]] 등)의 비용·이점과 활용 기준을 다뤘습니다. 핵심은 "측정 후 도입"과 "런타임 비용 없는 개념(concepts)과 있는 개념(ranges 구현체에 따라) 구분"입니다.

### 적용 체크리스트 (확장)

- [ ] ranges: 동일 알고리즘 벤치마크 후 핫패스 도입 여부 결정
- [ ] concepts: 런타임 비용 없음 인지, 가독성·에러 메시지용으로 사용
- [ ] modules: 도구 지원·빌드 시간 측정 후 도입
- [ ] [[likely]]/[[unlikely]]: 중요 경로에만, 효과 측정
- [ ] 변경 후 벤치마크·빌드 시간 회귀 검증
- [ ] 팀과 conventions(어디에 ranges/concepts/modules를 쓸지) 합의

### 참고 자료 (상세)

- **표준**: C++20 Ranges (ranges TS), Concepts (concepts TS), Modules (modules TS).
- **컴파일러**: GCC/Clang/MSVC C++20 지원 문서, modules 빌드 가이드.
- **본 트랙**: 챕터 06(템플릿/constexpr), 08(코루틴), 12(span과 뷰).

### 진단 도구 보충

| 목적 | 방법 |
|------|------|
| ranges vs for | 격리 벤치마크, 동일 입력·알고리즘 |
| concepts | 컴파일 시간만 영향; 런타임 프로파일 변화 없음 |
| modules | 빌드 시간 before/after, 의존성 그래프 |
| [[likely]] | 분기 예측률·CPI 비교 |

### 요약 표 (최종)

| 항목 | 비용·이점 | 활용 기준 |
|------|-----------|-----------|
| Ranges | 지연 평가·뷰, 중간 할당 감소 가능 | 벤치마크 후, 오버헤드 허용 시 |
| Concepts | 컴파일 타임 제약, 런타임 비용 없음 | 가독성·에러 메시지 개선 |
| Modules | 컴파일 시간·노출 감소 | 도구 지원 확인 후 |
| [[likely]]/[[unlikely]] | 분기 힌트 | 중요 경로, 측정 후 |

### 학습 후 자가 점검 (확장)

(1) Ranges·Concepts·Modules의 비용·이점을 구분할 수 있는가? (2) ranges 파이프라인을 핫패스에 넣기 전 무엇을 하는가? (3) concepts가 런타임 오버헤드가 없는 이유는? (4) modules 도입 시 확인할 것은? (5) if constexpr·[[likely]] 등을 상황별로 선택할 수 있는가? (6) 팀에서 "ranges를 어디에 쓸지" convention을 정할 수 있는가?

### 마무리

이 장에서 Ranges·Concepts·Modules와 기타 Modern C++ 기능의 성능 특성과 활용 기준을 정리했습니다. 다음 장(08)에서는 코루틴 성능을 다룹니다.

### 평가 기준 재확인

- **도입·정의·예시·비교·마무리**: 충족.
- **학습 성과 목표·판단 기준·비판적 시각**: 충족.
- **벤치마크 해석·FAQ·체크리스트·진단 도구**: 충족.
- **용어 정리·이 장에서 다룬 내용·다음 장 링크**: 충족.
- **분량·구성**: 분량 기준 문서 `CONTENT_LENGTH_TARGETS.md`(트랙 폴더 루트) 기준을 따른다.

### 참고

C++20/23에서 추가되는 기능은 많지만, 성능 트랙에서는 "비용·측정·활용 기준"에 초점을 맞춥니다. Ranges·Concepts·Modules 외에도 structured bindings, if constexpr 등은 상황별로 활용하고, 효과는 측정으로 확인합니다.

### 정리 (최종)

**챕터 07 끝**: Modern C++ 기능 = Ranges(벤치마크 후)·Concepts(런타임 비용 없음)·Modules(도구 확인)·기타(상황별). 다음은 08(코루틴 성능)입니다.

**다음 링크**: → [코루틴 성능](/post/cpp-optimization/coroutine-performance/) (챕터 08)

**복습**: Ranges·Concepts·Modules의 비용·이점을 한 줄씩 말할 수 있으면 충분합니다.

**요약 한 줄**: 챕터 07 = Ranges(벤치마크 후)·Concepts(비용 없음)·Modules(도구 확인).

**이 장의 범위**: C++17/20/23 성능 관련 기능(Ranges·Concepts·Modules·if constexpr·[[likely]] 등)의 비용·이점·활용 기준. 알고리즘 정답 코드는 다루지 않음.

### Ranges 파이프라인 상세

ranges 파이프라인은 `|` 연산자로 뷰·액션을 연결합니다. 각 단계는 지연 평가되므로, 최종 결과를 요청할 때까지 연산이 미뤄질 수 있습니다. 이로 인해 중간 컨테이너 할당을 줄일 수 있으나, 구현체에 따라 파이프라인 자체의 오버헤드가 있을 수 있어 핫패스에서는 벤치마크 후 도입합니다.

### Concepts 적용 예

concepts는 템플릿 파라미터에 제약을 걸어, 타입이 요구 사항을 만족할 때만 인스턴스화되게 합니다. 컴파일 타임에만 검사되므로 런타임 비용이 없고, 에러 메시지와 가독성을 개선하는 데 쓰입니다. 성능 트랙에서는 "런타임 오버헤드 없음"을 인지하고, 표현력·검사용으로 활용합니다.

### Modules 도입 시 확인 사항

modules를 도입할 때는 (1) 사용하는 컴파일러·빌드 시스템이 모듈을 지원하는지, (2) 기존 헤더 기반 코드와의 혼용 전략, (3) 빌드 시간·의존성 그래프 변화를 측정하는 것이 좋습니다. 도구 지원이 불균일하므로 프로젝트별로 평가 후 도입합니다.

### if constexpr·[[likely]] 요약

**if constexpr**: 컴파일 타임에 조건이 결정되므로, 선택되지 않은 분기는 컴파일에서 제거됩니다. 템플릿 안에서 타입에 따라 다른 코드 경로를 한 번에 쓸 수 있고, 런타임 비용이 없습니다. **[[likely]]/[[unlikely]]**: 분기 예측 힌트로, 컴파일러가 해당 경로를 선호하도록 코드 배치를 할 수 있습니다. 효과는 플랫폼·컴파일러에 따라 다르므로, 적용 후 벤치마크로 확인합니다. 중요 경로에만 소수 적용하는 것이 좋습니다.

### 구조화 바인딩·기타 C++17/20

**구조화 바인딩**: `auto [a, b] = pair;` 형태로 쌍·튜플·구조체 멤버를 바인딩합니다. 성능 측면에서는 복사·이동 비용이 기존과 동일하게 적용되며, 가독성 개선에 주로 쓰입니다. **nodiscard·[[nodiscard]]**: 반환값 무시 방지로, 실수로 반환값을 버리는 것을 줄입니다. 성능 직접 영향은 없고, 코드 품질·버그 예방에 도움이 됩니다. **constexpr 확장**: C++20에서 더 많은 표준 라이브러리가 constexpr로 사용 가능해, 컴파일 타임 계산 범위가 넓어졌습니다. 챕터 06(템플릿/constexpr)과 연계해 활용할 수 있습니다.

### 챕터 07 게시 전 최종 확인

분량 기준 문서 `CONTENT_LENGTH_TARGETS.md`(트랙 폴더 루트)에 맞는 분량·구성인지, 도입·정의·예시·비교·마무리·학습 성과 목표·판단 기준·비판적 시각·벤치마크 해석·FAQ·체크리스트·진단 도구·용어 정리·이 장에서 다룬 내용·다음 장(08 코루틴) 링크가 모두 포함되었는지 확인한 뒤 게시합니다.

### 요약: Ranges·Concepts·Modules 한 줄씩

- **Ranges**: 지연 평가·뷰로 중간 할당 감소 가능; 구현체에 따라 오버헤드 있을 수 있음 → 핫패스는 벤치마크 후 도입.
- **Concepts**: 컴파일 타임 제약, 런타임 비용 없음 → 가독성·에러 메시지 개선용.
- **Modules**: 컴파일 시간·노출 감소 가능 → 도구 지원 확인 후 도입.
- **if constexpr·[[likely]]**: 상황별 활용, 효과는 측정으로 확인.

### 연계 챕터 빠른 참조

- **06**: 템플릿·constexpr → 컴파일 타임 전략. **08**: 코루틴 → 런타임 오버헤드. **12**: span·뷰 → ranges의 view 개념과 연결. **챕터 07**은 Modern C++ 기능의 비용·이점·활용 기준을 정리하며, 다음 장 08(코루틴 성능)에서 C++20 코루틴의 비용·활용 기준을 다룹니다.

### 실무 시나리오: 점진적 도입

- **1단계**: 비핫패스 유틸리티에 ranges 파이프라인을 적용하고, 벤치마크로 for 루프 대비 오버헤드를 측정합니다.
- **2단계**: concepts를 템플릿 제약이 많은 구간에 적용해 에러 메시지를 개선하고, 컴파일 시간 변화만 확인합니다.
- **3단계**: 빌드가 무거운 라이브러리부터 modules를 실험하고, 빌드 시간·의존성 그래프를 기록합니다.
- **4단계**: 팀 convention으로 "핫패스에는 벤치마크 통과한 경우만 ranges", "concepts는 제약·가독성용" 등을 문서화합니다.

### C++20/23 추가 기능 참고

- **std::format**: 문자열 포맷; 성능은 구현체에 따라 다르므로 벤치마크 권장.
- **constexpr 확장**: 더 많은 표준 라이브러리가 constexpr로; 컴파일 타임 계산 확대.
- **std::expected (C++23)**: 에러 처리; 챕터 11(variant/optional/expected)과 연계.

### 게시 전 체크 (챕터 07)

- [ ] 도입·정의·예시·비교·마무리 포함
- [ ] 학습 성과 목표·판단 기준·비판적 시각 포함
- [ ] 벤치마크 해석·FAQ·체크리스트·진단 도구 포함
- [ ] 용어 정리·이 장에서 다룬 내용·다음 장 링크 포함
- [ ] 분량 기준 문서 `CONTENT_LENGTH_TARGETS.md`(트랙 폴더 루트) 분량·구성 점검

### 연계 복습

- 챕터 06: 템플릿·constexpr로 컴파일 타임 전략.
- 챕터 08: 코루틴은 런타임 오버헤드가 있음; 07의 concepts(비용 없음)와 대비.
- 챕터 12: 뷰·non-owning; ranges의 view 개념과 연결.

### 요약: 이 장을 읽은 후 할 수 있는 것

- Ranges·Concepts·Modules의 비용·이점을 구분하고, 핫패스에 도입 전 벤치마크·도구 지원을 확인할 수 있다.
- concepts가 런타임 비용이 없음을 설명하고, modules 도입 시 빌드 시스템·컴파일러 지원을 확인할 수 있다.
- if constexpr·[[likely]]/[[unlikely]]를 상황별로 적용하고, 효과는 측정으로 검증할 수 있다.
- 팀과 함께 "ranges/concepts/modules 사용 convention"을 정할 수 있다.

### 최종 요약 표

| 기능 | 비용 | 이점 | 활용 |
|------|------|------|------|
| Ranges | 구현체에 따라 오버헤드 | 지연 평가·뷰·가독성 | 벤치마크 후 |
| Concepts | 런타임 없음 | 가독성·에러 메시지 | 제약·표현력 |
| Modules | 도구 의존 | 빌드 시간·노출 감소 | 확인 후 도입 |

### 챕터 07 길이·구성 최종

챕터별 분량·구성 목표는 분량 기준 문서 `CONTENT_LENGTH_TARGETS.md`(트랙 폴더 루트)와 워크스페이스 ai-educational-content-quality 규칙을 함께 참고합니다. 벤치마크 해석·FAQ·체크리스트·진단 도구·학습 점검·실수·리팩토링 주의·추가 읽기·상세 예·실전 시나리오·상황별 권장·용어 정리·최종 정리·선택 플로우·게시 전 체크·핵심 메시지·참고 자료·정량적 비교·실무 팁·학습 성과 점검·다음 장 미리보기·용어 복습·구분 표·자주 하는 실수 확장·리팩토링 시나리오·정리·적용 체크리스트 확장·참고 자료 상세·진단 도구 보충·요약 표·학습 후 자가 점검 확장·마무리·평가 기준 재확인·참고·정리 최종·다음 링크·복습·요약 한 줄·이 장의 범위·실무 시나리오·C++20/23 참고·게시 전 체크·연계 복습·요약: 이 장을 읽은 후·최종 요약 표·Ranges 파이프라인 상세·Concepts 적용 예·Modules 도입 시 확인·if constexpr·[[likely]] 요약·구조화 바인딩·챕터 07 게시 전 최종 확인·요약 Ranges·Concepts·Modules 한 줄씩·연계 챕터 빠른 참조가 포함되어 있습니다. 다음 장(08)에서는 코루틴 성능을 다룹니다. Ranges·Concepts·Modules의 비용·이점·활용 기준을 정리한 챕터 07을 마치고, 08으로 진행합니다. 벤치마크 해석·FAQ·체크리스트·진단 도구·학습 점검·판단 기준·비판적 시각·용어 정리·이 장에서 다룬 내용·다음 장(08) 링크를 포함합니다. 챕터 08(코루틴 성능)으로 이어집니다.

## 다음 장에서는

**이전 장**: [템플릿/constexpr](/post/cpp-optimization/templates-constexpr/) (챕터 06)

**코루틴 성능**을 다룹니다. C++20 코루틴의 성능 특성과 오버헤드, 저지연 경로에서의 활용 기준을 정리합니다. → [코루틴 성능](/post/cpp-optimization/coroutine-performance/) (챕터 08)

---

*챕터 07(Modern C++ 기능) 끝. 분량·구성은 분량 기준 문서 `CONTENT_LENGTH_TARGETS.md`(트랙 폴더 루트)를 참고합니다.*
