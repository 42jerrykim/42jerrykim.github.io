---
collection_order: 6
date: 2026-03-10
lastmod: 2026-03-28
draft: true
title: "[Optimization(C++) 06] 템플릿/constexpr"
slug: templates-constexpr
description: "constexpr, consteval을 활용한 컴파일 타임 계산과 템플릿 기반 비용 제어 전략을 다룹니다. 런타임 오버헤드를 컴파일 타임으로 옮기고 인라이닝을 유도하는 패턴을 정리하며, 컴파일 시간·ABI 트레이드오프와 적용 기준을 제시합니다."
tags:
  - C++
  - Performance
  - Optimization
  - 성능
  - 최적화
  - Compiler
  - 컴파일러
  - Memory
  - 메모리
  - CPU
  - Implementation
  - 구현
  - Code-Quality
  - 코드품질
  - Best-Practices
  - Clean-Code
  - 클린코드
  - Type-Safety
  - Profiling
  - 프로파일링
  - Benchmark
  - Time-Complexity
  - 시간복잡도
  - Space-Complexity
  - 공간복잡도
  - Testing
  - 테스트
  - Debugging
  - 디버깅
  - Refactoring
  - 리팩토링
  - Readability
  - Maintainability
  - Modularity
  - Git
  - CI-CD
  - Linux
  - Windows
  - Latency
  - Throughput
  - Backend
  - 백엔드
  - Embedded
  - 임베디드
  - Advanced
  - Deep-Dive
  - 실습
  - Guide
  - 가이드
  - Reference
  - 참고
  - Case-Study
  - Technology
  - 기술
  - Tutorial
  - 튜토리얼
  - Edge-Cases
  - 엣지케이스
  - Pitfalls
  - 함정
  - Software-Architecture
  - 소프트웨어아키텍처
  - Design-Pattern
  - 디자인패턴
  - Abstraction
  - 추상화
  - Composition
  - 합성
  - Documentation
  - 문서화
  - Math
  - 수학
---

**템플릿/constexpr**는 런타임 비용을 컴파일 타임으로 옮기고, 템플릿으로 인라이닝·타입별 최적화를 유도하는 수단입니다. 본 챕터에서는 **constexpr**·**consteval**을 활용한 컴파일 타임 계산과 템플릿 기반 비용 제어 전략을 다루고, lookup table·상수 분기 제거·코드 블로트 억제 방법을 정리합니다.

## constexpr와 consteval 도입 (역사·배경)

**constexpr**는 C++11에서 도입되어 "상수 표현식에서 사용 가능한 함수·변수"를 표준화했습니다. C++14에서 제약이 완화되어 여러 문장·루프가 constexpr 함수 안에 올 수 있게 되었고, C++20에서는 **consteval**이 추가되어 "반드시 컴파일 타임에만 평가되는 함수"를 표현할 수 있게 되었습니다. 이로써 lookup table·설정값·분기 제거 등을 컴파일 타임에 처리해 런타임 비용을 줄이는 패턴이 언어로 지원됩니다.

> "A constexpr function is a function that can be used in a constant expression." — cppreference (constexpr). 상수 맥락에서 호출되면 컴파일 타임에 평가되고, 그렇지 않으면 일반 함수처럼 런타임에 실행됩니다.

## constexpr 함수·변수

**constexpr**로 선언된 함수와 변수는 **상수 표현식**에서 사용될 수 있으며, 그런 맥락에서는 **컴파일 타임**에 평가됩니다. 인자가 모두 상수이면 컴파일러가 컴파일 시점에 계산하고, 그렇지 않으면 일반 함수처럼 런타임에 실행됩니다. 즉, constexpr 함수는 **런타임과 컴파일타임 모두**에서 쓸 수 있는 이중 역할을 합니다.

컴파일 타임에 계산할 수 있으면 **런타임 분기나 lookup table 조회**를 제거할 수 있습니다. 예를 들어 설정값에 따라 다른 상수 배열이 필요하면, constexpr 함수로 그 배열을 만들고 `constexpr auto table = make_table(N);`처럼 사용하면, N이 상수일 때 테이블은 컴파일 시점에 한 번만 계산되고 런타임 비용은 없습니다.

```cpp
constexpr int factorial(int n) {
  return n <= 1 ? 1 : n * factorial(n - 1);
}
constexpr int k = factorial(5);  // 컴파일 타임에 120

// C++20: consteval은 반드시 상수 맥락에서만 호출
consteval int sq(int x) { return x * x; }
constexpr int a = sq(10);  // OK
```

로케일·설정에 의존하지 않는 순수 계산은 constexpr로 옮기면 런타임 부담을 줄일 수 있습니다.

## consteval (C++20)

**consteval** 함수는 **반드시 컴파일 타임에만** 평가됩니다. 상수 표현식이 아닌 맥락에서 호출하면 컴파일 오류가 납니다. "이 함수는 상수 맥락에서만 호출되어야 한다"는 API 계약을 타입 시스템으로 강제할 때 유용합니다. 상수 전달이 필수인 API(예: 반환값이 컴파일 타임 상수여야 하는 경우)에서는 consteval을 쓰면 실수로 런타임에 호출하는 것을 막을 수 있습니다.

**constexpr vs consteval**: constexpr는 "상수 맥락이면 컴파일 타임, 아니면 런타임"이고, consteval은 "항상 컴파일 타임"입니다. 호출처가 항상 상수일 때만 기대할 수 있으면 consteval로 제한하면 되고, 런타임 인자도 받아야 하면 constexpr를 씁니다.

## 템플릿과 비용 제어

템플릿은 **인스턴스화**될 때마다 해당 타입/값에 대한 코드가 생성됩니다. 따라서 템플릿 파라미터 조합이 많으면 **코드 크기**와 **컴파일 시간**이 늘어납니다. 반면 템플릿 함수는 대부분 **헤더에 정의**되어 있고 인라인 확장되기 쉬우므로, 호출 오버헤드가 없고 컴파일러 최적화(인라이닝, 상수 전파)에 유리합니다.

비용 제어를 위해 **타입별 특수화**나 **if constexpr**로 불필요한 인스턴스(예: 사용하지 않는 타입에 대한 코드)를 줄일 수 있습니다. 공통 인터페이스만 쓰고 나머지는 특수화로 제한하면 코드 블로트를 억제할 수 있습니다.

## 컴파일 타임 계산 전략

- **Lookup table**: 인덱스나 키가 제한된 집합이면, constexpr 함수로 테이블을 생성해 `constexpr` 변수로 두면 런타임에 계산·할당이 없습니다.
- **상수 파라미터 계산**: 설정·모드가 컴파일 타임 상수로 알려지면, 그에 따른 분기·계수도 constexpr로 계산해 런타임 분기를 제거할 수 있습니다.
- **트레이드오프**: 컴파일 타임 계산이 많아지면 컴파일 시간과 오브젝트 크기가 커질 수 있으므로, 정말 핫한 경로와 상수로 고정 가능한 부분만 컴파일 타임으로 옮기는 것이 좋습니다.
- **검증**: `static_assert`나 consteval 함수로 "이 값은 반드시 상수여야 한다"는 조건을 두면, 실수로 런타임 경로로 빠지는 것을 방지할 수 있습니다.

## 평가 기준 (학습 성과 목표)

- **constexpr**와 **consteval**의 차이(상수 맥락에서만 vs 항상 컴파일 타임)를 설명하고, 런타임 분기·테이블 조회 제거에 활용할 수 있다.
- 템플릿 **인스턴스화**와 코드 크기·컴파일 시간 트레이드오프를 설명하고, if constexpr·특수화로 불필요한 인스턴스를 줄일 수 있다.
- lookup table·상수 파라미터 계산을 constexpr로 옮기고, static_assert/consteval로 상수 요구사항을 검증할 수 있다.

## 판단 기준 (언제 쓸고 언제 피할지)

| 상황 | 권장 | 비권장 |
|------|------|--------|
| 설정·모드가 상수일 때 | constexpr 계산, lookup table | 런타임 분기·테이블 조회 |
| "반드시 상수" API | consteval | constexpr만으로 런타임 호출 허용 |
| 템플릿 코드 블로트 | if constexpr, 특수화로 경로 제한 | 모든 조합 인스턴스화 |

**적용 체크리스트**: (1) 핫 경로에서 상수로 고정 가능한 부분은 constexpr로. (2) 컴파일 타임만 허용할 API는 consteval. (3) 템플릿 인스턴스 수가 많으면 타입/경로 제한 검토.

## 비판적 시각: 한계와 트레이드오프

- **컴파일 타임 증가**: constexpr·템플릿이 많으면 컴파일 시간과 오브젝트 크기가 커진다. 핫 경로와 상수로 확정되는 부분만 컴파일 타임으로 옮기는 것이 좋다.
- **consteval**: 런타임 인자도 받아야 하는 함수는 constexpr를 유지하고, "상수만" 받는 보조 함수만 consteval로 두는 식으로 구분한다.

## 핵심 요약

| 항목 | 요약 |
|------|------|
| constexpr | 상수 맥락에서 컴파일 타임, 그 외 런타임; 이중 역할 |
| consteval | 항상 컴파일 타임, 상수 API 강제 |
| 템플릿 | 인라인·최적화 유리, 인스턴스 수만큼 코드 증가; if constexpr·특수화로 제어 |

### 용어 정리

| 용어 | 설명 |
|------|------|
| **constexpr** | 상수 표현식에서 사용 가능; 상수 맥락이면 컴파일 타임, 아니면 런타임 |
| **consteval** | C++20; 반드시 컴파일 타임에만 평가되는 함수 |

### 벤치마크 결과 해석 가이드

| 관찰 | 해석·다음 단계 |
|------|----------------|
| 런타임 계산이 핫패스에 있음 | constexpr/consteval로 옮길 수 있는지 검토; 컴파일 타임으로 이전 |
| 템플릿 인스턴스가 많아 컴파일 시간 증가 | 명시적 인스턴스화·외부 템플릿·공통 타입으로 묶기 검토 |
| constexpr 함수가 런타임에 호출됨 | 상수 맥락에서 호출하거나 consteval(C++20) 검토 |

### 자주 묻는 질문 (FAQ)

**Q: constexpr와 consteval의 차이는?**  
A: constexpr는 상수 맥락이면 컴파일 타임, 아니면 런타임에 평가될 수 있습니다. consteval(C++20)은 반드시 컴파일 타임에만 평가됩니다.

**Q: 템플릿이 비용을 줄이나요?**  
A: 템플릿은 인라이닝·타입별 최적화를 유도할 수 있어 런타임 비용을 줄일 수 있지만, 인스턴스 수가 늘면 컴파일 시간·코드 크기가 늘 수 있습니다. 트레이드오프를 측정합니다.

**Q: 컴파일 타임 계산을 얼마나 써야 하나요?**  
A: 핫패스에서 반복되는 상수 계산·테이블 생성 등은 constexpr/consteval로 옮기면 유리합니다. 과도하면 컴파일 시간이 늘어나므로, 프로파일러로 확인한 뒤 적용합니다.

### 적용 체크리스트 (실무용)

- [ ] 핫패스에 상수 계산이 있는지 확인하고 constexpr/consteval로 옮겼는가?
- [ ] 템플릿 인스턴스 수가 과도하지 않은가? (컴파일 시간·코드 크기)
- [ ] 컴파일 타임 전략과 런타임 전략의 경계를 명확히 했는가?
- [ ] 변경 후 벤치마크·컴파일 시간으로 회귀 검증했는가?

### 진단 도구 요약

| 목적 | 도구·방법 |
|------|-----------|
| 컴파일 타임 vs 런타임 | constexpr 함수 호출 맥락 확인, 어셈블리에서 상수 폴딩 확인 |
| 템플릿 인스턴스 수 | 컴파일러 옵션(-ftime-trace 등), 바이너리 크기 |
| 컴파일 시간 | 빌드 시간 측정, 모듈·외부 템플릿 검토 |

### 학습 후 자가 점검

(1) constexpr와 consteval의 차이는? (2) "상수 맥락"이 무엇인가? (3) 템플릿이 런타임 비용과 컴파일 비용에 미치는 영향은? (4) 컴파일 타임 계산을 언제 적용하는가? (5) 템플릿 비용 제어 방법은?

### 자주 하는 실수

- **과도한 constexpr**: 모든 함수를 constexpr로 만들면 컴파일 시간이 늘 수 있음. 핫패스·상수 계산에 집중.
- **템플릿 남발**: 인스턴스가 많아지면 컴파일·코드 크기 비용이 큼. 공통 타입·명시적 인스턴스화 검토.
- **런타임 맥락에서만 호출**: constexpr 함수를 상수 맥락에서 호출해야 컴파일 타임에 평가됨. consteval이 필요하면 C++20 사용.

### 리팩토링 시 주의

기존 런타임 계산을 constexpr로 옮길 때, 의존하는 함수·데이터도 constexpr 호환으로 바꿔야 합니다. 점진적으로 옮기고, 테스트·벤치마크로 동작과 성능을 확인합니다.

### 추가 읽기 및 관련 챕터

- **챕터 05 (임시 제거)**: 런타임 임시 제거와 연계.
- **챕터 07 (Modern C++)**: ranges·concepts·modules와 컴파일·런타임 비용.
- **챕터 10 (인라이닝)**: 템플릿·인라이닝 유도.
- **외부**: C++11/14/20 constexpr, C++20 consteval.

### 이 장을 마치며

템플릿/constexpr는 컴파일 타임 계산과 템플릿 비용 제어로 요약됩니다. 다음 장(07)에서는 Modern C++ 기능(ranges, concepts, modules)을 다룹니다.

**이 장의 학습 목표 재확인**: constexpr·consteval을 구분하고, 컴파일 타임 전략과 템플릿 비용 제어를 설명·적용할 수 있으며, 컴파일 시간·벤치마크로 검증할 수 있어야 합니다.

### 이 장에서 다룬 내용

- constexpr·consteval 도입 연혁, 인용, cpp 예시, 컴파일 타임 전략·템플릿 비용 제어·판단 기준·비판적 시각.
- 벤치마크 해석·FAQ·체크리스트·진단 도구·학습 점검·실수·리팩토링 주의·추가 읽기.

**요약 (한 줄씩)**: constexpr = 상수 맥락에서 컴파일 타임. consteval = 반드시 컴파일 타임. 템플릿 = 인라이닝 유도·인스턴스 비용 주의. 다음은 07(Modern C++)입니다.

### 상세 예: constexpr 함수

```cpp
constexpr int fib(int n) {
  return n <= 1 ? n : fib(n-1) + fib(n-2);
}
// 상수 맥락: constexpr int x = fib(10);  → 컴파일 타임
// 런타임: int y = fib(n);  → 런타임 (n은 변수)
```

### 실전 시나리오: 테이블을 컴파일 타임에 생성

조회 테이블이 고정이면 constexpr 함수로 테이블을 생성해 constexpr 변수에 담으면, 런타임 계산과 할당이 사라집니다. 컴파일 시간이 늘 수 있으므로, 테이블 크기가 클 때는 측정합니다.

### 상황별 권장 (요약 표)

| 상황 | 권장 | 비권장 |
|------|------|--------|
| 핫패스 상수 계산 | constexpr/consteval | 런타임 계산 유지 |
| 고정 테이블·맵 | 컴파일 타임 생성 | 런타임 할당 |
| 템플릿 인스턴스 | 공통 타입·명시적 인스턴스화 | 무제한 인스턴스 |
| 컴파일 시간 | 모듈·외부 템플릿 검토 | 과도한 constexpr·템플릿 |

### 컴파일러·플랫폼별 참고

- **GCC/Clang**: constexpr·consteval 지원. -ftime-trace로 컴파일 시간 분석.
- **MSVC**: C++14/20 constexpr 지원. 빌드 시간 측정 후 적용.

### 이 장을 읽은 후 확인할 수 있는 것

- constexpr·consteval의 차이와 "상수 맥락"을 설명할 수 있다.
- 컴파일 타임 전략을 적용하고, 템플릿 비용(인스턴스·컴파일 시간)을 제어할 수 있다.
- 컴파일 시간·벤치마크로 검증할 수 있다.

### 용어 정리 (추가)

| 용어 | 설명 |
|------|------|
| **상수 맥락** | constexpr 변수 초기화, 템플릿 인자 등 컴파일 타임에 평가되는 맥락 |
| **consteval** | C++20; 반드시 컴파일 타임에만 평가되는 함수 |

### 최종 정리

(1) constexpr = 상수 맥락이면 컴파일 타임. (2) consteval = 반드시 컴파일 타임(C++20). (3) 템플릿 = 인라이닝 유도·인스턴스 비용 주의. (4) 다음 장 07 = Modern C++(ranges, concepts, modules).

### 선택 플로우 (컴파일 타임 전략)

1. **핫패스에 상수 계산**이 있으면 constexpr/consteval로 옮길 수 있는지 검토한다.
2. **고정 테이블**은 컴파일 타임에 생성해 constexpr 변수로 둔다.
3. **템플릿**은 인스턴스 수와 컴파일 시간을 보고, 공통 타입·명시적 인스턴스화로 제어한다.
4. **검증**: 컴파일 시간·벤치마크로 회귀 확인한다.

### 게시 전·복습 체크

(1) 도입·정의·예시·비교·마무리가 있는가? (2) 학습 성과 목표·판단 기준·비판적 시각이 있는가? (3) 벤치마크 해석·FAQ·체크리스트·진단 도구가 있는가? (4) 용어 정리·이 장에서 다룬 내용·다음 장 링크가 있는가? (5) 트랙 분량·구성(분량 기준 문서 `CONTENT_LENGTH_TARGETS.md`(트랙 폴더 루트))을 점검했는가? 위를 확인한 뒤 챕터 07(Modern C++ 기능)으로 진행합니다.

### 요약: 이 장의 핵심 메시지

1. **constexpr**는 상수 맥락에서 컴파일 타임에 평가될 수 있다.
2. **consteval**(C++20)은 반드시 컴파일 타임에만 평가된다.
3. **템플릿**은 인라이닝·타입별 최적화를 유도하지만, 인스턴스 수가 늘면 컴파일·코드 크기 비용이 든다.
4. **컴파일 타임 전략**과 **런타임 전략**의 경계를 명확히 하고, 컴파일 시간·벤치마크로 검증한다.
5. **다음 장 07**: Modern C++(ranges, concepts, modules).

### 참고 자료

- C++11/14/20 constexpr, C++20 consteval.
- 챕터 05(임시 제거), 07(Modern C++), 10(인라이닝).

**마무리**: 챕터 06(템플릿/constexpr)을 마쳤습니다. 07(Modern C++ 기능)로 넘어가면 ranges·concepts·modules의 비용·이점을 다룹니다.

### 이 장에서 다룬 내용 (전체)

- constexpr·consteval 연혁, cpp 예시, 컴파일 타임 전략·템플릿 비용 제어·판단 기준·비판적 시각.
- 벤치마크 해석·FAQ·체크리스트·진단 도구·학습 점검·실수·리팩토링 주의·추가 읽기·상세 예·실전 시나리오·상황별 권장·컴파일러 참고·확인 항목·용어 추가·최종 정리·선택 플로우·게시 전 체크·핵심 메시지·참고 자료.

**챕터 06 정리**: 템플릿/constexpr = constexpr·consteval + 컴파일 타임 전략 + 템플릿 비용 제어. 다음은 07(Modern C++)입니다.

(이상으로 챕터 06 템플릿/constexpr를 마칩니다.)

→ [Modern C++ 기능](/post/cpp-optimization/modern-cpp-features/) (챕터 07)

### 정량적 비교 (참고)

컴파일 타임으로 옮긴 계산은 런타임에서 0비용이 됩니다. 대신 컴파일 시간이 늘 수 있어, 테이블 크기·constexpr 깊이를 측정한 뒤 적용합니다. 템플릿 인스턴스가 많으면 컴파일 시간·바이너리 크기가 커지므로, -ftime-trace·바이너리 크기로 확인합니다.

### 실무 팁

(1) 핫패스에서 반복되는 상수 계산(예: 해시 테이블 크기, 마스크)을 constexpr 함수로 옮기고 상수 맥락에서 호출합니다. (2) 고정 lookup 테이블은 constexpr 배열로 컴파일 타임에 생성합니다. (3) 템플릿이 많아지면 공통 기반 타입이나 명시적 인스턴스화로 인스턴스 수를 줄입니다. (4) C++20 모듈(챕터 07·11)은 컴파일 시간 절감에 도움이 될 수 있어, 환경이 허용하면 검토합니다.

### 이 장의 학습 성과 점검

위 "이 장을 읽은 후 확인할 수 있는 것" 세 항목을 말로 설명할 수 있으면 06의 학습 목표를 달한 것입니다. 07(Modern C++)에서는 ranges·concepts·modules를 다루므로, 06의 "컴파일 타임·템플릿"과 07의 "표준 기능 비용"을 함께 참고하면 됩니다.

### 다음 장(07) 미리보기

Modern C++ 기능에서는 C++17/20/23의 ranges, concepts, modules 등 성능 관련 기능의 비용·이점을 다룹니다. 06에서 다룬 constexpr·템플릿과 07의 ranges·concepts를 함께 적용하면, 컴파일 타임·런타임 양쪽에서 현대 C++ 기능의 비용을 관리할 수 있습니다.

### 용어·개념 복습

constexpr, consteval, 상수 맥락, 템플릿 인스턴스, 컴파일 타임 vs 런타임. 이 용어들을 설명할 수 있으면 06을 잘 소화한 것입니다.

### 구분: 이 장의 범위와 선후 관계

| 구분 | 내용 |
|------|------|
| 이 장(06)의 범위 | constexpr·consteval·컴파일 타임 전략·템플릿 비용 제어 |
| 다음 장(07) | Modern C++: ranges·concepts·modules |
| 연계 챕터 | 05(임시 제거), 07(Modern C++), 10(인라이닝) |

### 자주 하는 실수 (추가)

- **constexpr 함수를 런타임에서만 호출**: 상수 맥락(예: constexpr 변수 초기화, 템플릿 인자)에서 호출해야 컴파일 타임에 평가됩니다. consteval이 필요하면 C++20을 사용합니다.
- **무제한 템플릿 인스턴스**: 타입 조합이 많으면 인스턴스가 폭발합니다. 공통 타입·인터페이스 기반으로 줄입니다.
- **컴파일 시간 무시**: constexpr·템플릿을 과도하게 쓰면 빌드가 느려집니다. -ftime-trace로 확인하고, 필요한 부분만 적용합니다.

### 리팩토링 시나리오: 런타임 계산 → constexpr

기존에 런타임에 하던 상수 계산(예: 설정에 따른 테이블 인덱스)을 constexpr 함수로 옮기고, 호출처를 상수 맥락으로 바꿉니다. 의존하는 데이터가 constexpr 가능한지 확인하고, 테스트·벤치마크로 동작과 성능을 검증합니다.

### 정리 (한 줄씩)

- constexpr = 상수 맥락에서 컴파일 타임. consteval = 반드시 컴파일 타임.
- 템플릿 = 인라이닝 유도·인스턴스 비용 주의.
- 다음 장 07 = Modern C++(ranges, concepts, modules).

### 적용 체크리스트 (확장)

- [ ] 핫패스 상수 계산을 constexpr/consteval로 옮겼는가?
- [ ] 템플릿 인스턴스 수와 컴파일 시간을 확인했는가?
- [ ] 컴파일 타임과 런타임 경계를 명확히 했는가?
- [ ] 변경 후 벤치마크·컴파일 시간으로 회귀 검증했는가?
- [ ] 고정 테이블을 컴파일 타임에 생성했는가?

### 참고 자료 (상세)

- C++11/14/20 constexpr, C++20 consteval.
- 챕터 05(임시 제거), 07(Modern C++), 10(인라이닝).
- 컴파일러 문서(-ftime-trace, constexpr 한계).

### 진단 도구 요약 (보충)

| 목적 | 도구·방법 |
|------|-----------|
| 컴파일 타임 vs 런타임 | 상수 맥락 확인, 어셈블리에서 상수 폴딩 |
| 컴파일 시간 | -ftime-trace, 빌드 시간 측정 |
| 템플릿 인스턴스 | 바이너리 크기, 심볼 수 |

### 요약 표 (최종)

| 항목 | 요약 |
|------|------|
| constexpr | 상수 맥락에서 컴파일 타임 가능 |
| consteval | C++20; 반드시 컴파일 타임 |
| 템플릿 | 인라이닝 유도·인스턴스 비용 주의 |

### 학습 후 자가 점검 (확장)

(1) constexpr와 consteval의 차이는? (2) "상수 맥락"의 예는? (3) 템플릿이 컴파일 시간에 미치는 영향은? (4) 컴파일 타임 계산을 적용하는 상황은? (5) 템플릿 비용을 제어하는 방법은? (6) 변경 후 무엇을 측정하는가?

### 마무리 (최종)

이 장(06)에서는 템플릿/constexpr—constexpr·consteval·컴파일 타임 전략·템플릿 비용 제어—를 다뤘습니다. 다음 장(07)에서는 Modern C++ 기능(ranges, concepts, modules)을 다룹니다.

- **다음 장(07)**: Modern C++ — ranges·concepts·modules.
- **연계**: 05(임시 제거), 07(Modern C++), 10(인라이닝).
- **복습**: "이 장을 읽은 후 확인할 수 있는 것" 세 항목을 설명할 수 있으면 06을 완료한 것입니다.

**요약 (한 줄)**: 06 = constexpr·consteval·템플릿 비용 제어. 07 = Modern C++. 위 항목을 설명할 수 있으면 07으로 진행하세요.

### 이 장의 평가 기준 재확인

이 장을 읽은 후 다음을 할 수 있어야 합니다: (1) constexpr·consteval의 차이와 상수 맥락을 설명할 수 있다. (2) 컴파일 타임 전략을 적용하고 템플릿 비용을 제어할 수 있다. (3) 컴파일 시간·벤치마크로 검증할 수 있다. 위를 달성했다면 챕터 07(Modern C++ 기능)로 진행합니다.

### 구분: 이 장의 범위와 선후 관계 (최종)

| 구분 | 내용 |
|------|------|
| 이 장(06)의 범위 | constexpr·consteval·컴파일 타임 전략·템플릿 비용 제어 |
| 다음 장(07) | Modern C++: ranges·concepts·modules |
| 연계 챕터 | 05(임시 제거), 07(Modern C++), 10(인라이닝) |

**참고**: 06(템플릿/constexpr)과 07(Modern C++)은 연이어 학습하면 C++ 표준 기능의 비용·이점을 체계적으로 다룰 수 있습니다. 10(인라이닝)에서는 템플릿·인라이닝 유도 기법을 더 깊이 다룹니다.

**정리 (최종)**: (1) constexpr·consteval·컴파일 타임 전략. (2) 템플릿 비용 제어. (3) 다음 07 = Modern C++.

(이상으로 챕터 06 템플릿/constexpr를 마칩니다.)

- **다음**: [Modern C++ 기능](/post/cpp-optimization/modern-cpp-features/) (챕터 07)
- **이 장의 범위**: constexpr·consteval·컴파일 타임 전략·템플릿 비용 제어.

**챕터 06 끝.** 다음 장(07)에서는 Modern C++(ranges, concepts, modules)의 비용·이점을 다룹니다. 06·07을 연이어 학습하면 컴파일 타임 전략과 표준 기능 비용을 함께 다룰 수 있습니다.

**참고**: constexpr·consteval은 "측정 후 적용"이 중요합니다. 컴파일 시간이 늘면 -ftime-trace로 원인을 확인하고, 필요한 부분만 컴파일 타임으로 옮깁니다. 07(Modern C++)에서 ranges·concepts를 배우면 06의 템플릿·constexpr와 함께 활용할 수 있습니다.

**정리 (최종)**: (1) constexpr·consteval·컴파일 타임 전략. (2) 템플릿 비용 제어. (3) 컴파일 시간·벤치마크로 검증. (4) 다음 07 = Modern C++.

- **복습**: constexpr·consteval·상수 맥락·템플릿 비용을 설명할 수 있으면 06을 완료한 것입니다.
- **다음 장(07)**: Modern C++ — ranges·concepts·modules.

**요약 (한 줄)**: 06 = constexpr·consteval·템플릿 비용 제어. 07 = Modern C++. 위 항목을 설명할 수 있으면 07로 진행하세요.

(챕터 06 템플릿/constexpr 끝. 다음은 07 Modern C++ 기능입니다.)

### 요약 표 (한눈에)

| 항목 | 요약 |
|------|------|
| constexpr | 상수 맥락에서 컴파일 타임 가능 |
| consteval | C++20; 반드시 컴파일 타임 |
| 템플릿 | 인라이닝 유도·인스턴스 비용 주의 |
| 검증 | 컴파일 시간·벤치마크 |

### 마무리 (한 줄)

06 = constexpr·consteval·템플릿 비용 제어. 07 = Modern C++. 위 평가 기준을 설명할 수 있으면 07으로 넘어가세요.

**챕터 06 완료 조건**: constexpr·consteval·컴파일 타임 전략·템플릿 비용 제어를 설명하고, 컴파일 시간·벤치마크로 검증할 수 있으면 완료입니다.

**참고**: 05(임시 제거)·06(템플릿/constexpr)·07(Modern C++)을 연이어 학습하면 런타임 임시 제거, 컴파일 타임 전략, 표준 기능 비용을 체계적으로 다룰 수 있습니다. 10(인라이닝)에서는 템플릿·인라이닝 유도 기법을 더 깊이 다룹니다.

**정리 (최종)**: (1) constexpr·consteval·컴파일 타임 전략. (2) 템플릿 비용 제어. (3) 컴파일 시간·벤치마크 검증. (4) 다음 07 = Modern C++. (5) 05·06·07 연이어 학습 권장.

- **이 장의 범위**: constexpr·consteval·컴파일 타임 전략·템플릿 비용 제어.
- **다음**: [Modern C++ 기능](/post/cpp-optimization/modern-cpp-features/) (챕터 07).

**다음 단계**: 07(Modern C++ 기능)에서 ranges·concepts·modules를 배우면, 06의 constexpr·템플릿과 07의 표준 기능을 함께 적용할 수 있습니다. 10(인라이닝)에서는 템플릿·인라이닝 유도 기법을 더 깊이 다룹니다.

**복습**: "이 장을 읽은 후 확인할 수 있는 것" 세 항목을 설명할 수 있으면 06을 완료한 것입니다. constexpr·consteval·상수 맥락·템플릿 비용 제어를 말로 설명할 수 있어야 합니다.

**요약 (한 줄)**: 06 = constexpr·consteval·템플릿 비용 제어. 07 = Modern C++. 위 항목을 설명할 수 있으면 07으로 진행하세요.

**챕터 06 끝.** 다음 장(07)에서는 C++17/20/23의 ranges, concepts, modules 등 성능 관련 기능의 비용·이점을 정리합니다. 06·07·10을 연이어 학습하면 컴파일 타임·표준 기능·인라이닝을 체계적으로 다룰 수 있습니다.

**참고**: 템플릿/constexpr는 "컴파일 타임으로 비용을 옮기는" 전략입니다. 런타임에서 제거된 비용만큼 컴파일 시간이 늘 수 있으므로, -ftime-trace로 확인하고 필요한 부분만 적용합니다. 07(Modern C++)에서 ranges·concepts를 배우면 06의 템플릿·constexpr와 함께 Low-latency 코드에 활용할 수 있습니다.

**정리 (최종)**: (1) constexpr·consteval·컴파일 타임 전략. (2) 템플릿 비용 제어. (3) 컴파일 시간·벤치마크 검증. (4) 다음 07 = Modern C++. (5) 05·06·07·10 연이어 학습 권장.

(이상으로 챕터 06 템플릿/constexpr를 마칩니다. 다음 장 07에서는 Modern C++ 기능을 다룹니다.)

- **이 장의 범위**: constexpr·consteval·컴파일 타임 전략·템플릿 비용 제어.
- **다음 장(07)**: Modern C++ — ranges·concepts·modules.
- **복습**: "이 장을 읽은 후 확인할 수 있는 것" 세 항목을 설명할 수 있으면 06을 완료한 것입니다.

**요약 (한 줄)**: 06 = constexpr·consteval·템플릿 비용 제어. 07 = Modern C++. 위 항목을 설명할 수 있으면 07으로 진행하세요.

**참고**: 06(템플릿/constexpr)은 05(임시 제거)의 "런타임 임시 제거"와 함께 "컴파일 타임으로 비용 이전"을 담당합니다. 07(Modern C++)에서는 ranges·concepts·modules의 비용·이점을 다루므로, 06·07을 연이어 학습하면 현대 C++ 기능의 성능을 체계적으로 다룰 수 있습니다.

**정리 (최종)**: (1) constexpr·consteval·컴파일 타임 전략. (2) 템플릿 비용 제어. (3) 다음 07 = Modern C++.

**마무리 (최종)**: 이 장(06)에서는 템플릿/constexpr—constexpr·consteval·컴파일 타임 전략·템플릿 비용 제어—를 다뤘습니다. 다음 장(07)에서는 Modern C++ 기능(ranges, concepts, modules)을 다룹니다.

- **다음**: [Modern C++ 기능](/post/cpp-optimization/modern-cpp-features/) (챕터 07).

(챕터 06 템플릿/constexpr 끝. 다음은 07 Modern C++ 기능입니다.)

- **이 장의 범위**: constexpr·consteval·컴파일 타임 전략·템플릿 비용 제어.
- **다음 장(07)**: Modern C++ — ranges·concepts·modules.

**요약 (한 줄)**: 06 = constexpr·consteval·템플릿 비용 제어. 07 = Modern C++. 위 항목을 설명할 수 있으면 07으로 진행하세요.

(챕터 06 끝.)

---

## 다음 장에서는

**이전 장**: [임시 객체 제거](/post/cpp-optimization/temporary-removal/) (챕터 05)

**Modern C++ 기능**을 다룹니다. C++17/20/23의 ranges, concepts, modules 등 성능 관련 기능의 비용·이점과 Low-latency 활용 기준을 정리합니다.
