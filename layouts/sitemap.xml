{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- $pages := where .Pages "Sitemap.Disable" "ne" true -}}
  {{- $pages = where $pages "Kind" "!=" "taxonomy" -}}
  {{- $pages = where $pages "Kind" "!=" "term" -}}
  {{- range $p := $pages -}}
    {{- if $p.Permalink -}}
    <url>
      <loc>{{ $p.Permalink }}</loc>
      {{- if not $p.Lastmod.IsZero -}}<lastmod>{{ safeHTML ( $p.Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{- end -}}
      {{- with $p.Sitemap.ChangeFreq -}}<changefreq>{{ . }}</changefreq>{{- end -}}
      {{- if ge $p.Sitemap.Priority 0.0 -}}<priority>{{ $p.Sitemap.Priority }}</priority>{{- end -}}
      {{- if $p.IsTranslated -}}
        {{- range $p.Translations -}}<xhtml:link rel="alternate" hreflang="{{ .Language.LanguageCode }}" href="{{ .Permalink }}" />{{- end -}}
        <xhtml:link rel="alternate" hreflang="{{ $p.Language.LanguageCode }}" href="{{ $p.Permalink }}" />
      {{- end -}}
    </url>
    {{- end -}}
  {{- end -}}
</urlset>
